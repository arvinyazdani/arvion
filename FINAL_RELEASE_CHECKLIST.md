# چک‌لیست نهایی انتشار Rvion

## وضعیت کد

- [x] migrationها ساخته شده و drift ندارند
- [x] تست‌های واحد، integration و journey اجرا می‌شوند
- [x] ممیزی ساختاری بانک سؤال در گیت انتشار است
- [x] collectstatic به‌صورت dry-run در گیت انتشار بررسی می‌شود
- [x] سازگاری dependencyهای نصب‌شده با `pip check` بررسی می‌شود
- [x] تنظیمات production در نبود secret یا سرویس ضروری fail closed است
- [x] health endpoint به دیتابیس وابسته و بدون افشای خطاست
- [x] نقش‌های حداقلی کارکنان و سفارش CRM در deploy ساخته/به‌روزرسانی می‌شوند
- [x] CI روی دو نسخه Python اجرا می‌شود
- [x] ممیزی code-level دسترس‌پذیری انجام شده است

## اطلاعاتی که مالک پروژه باید پیش از انتشار وارد کند

- [ ] دامنه نهایی و DNS
- [ ] `DJANGO_SECRET_KEY` تصادفی حداقل ۵۰ کاراکتر
- [ ] PostgreSQL production با TLS، backup و آزمون restore
- [ ] SMTP واقعی و تأیید دامنه فرستنده
- [ ] bucket و دامنه رسانه S3-compatible
- [x] روش موقت کارت‌به‌کارت و تأیید دستی ادمین
- [x] بسته Ubuntu/systemd/Nginx و health probe برای `/health/`
- [x] پایش first-party بازدید و کاربران فعال
- [ ] سرویس ثبت خطا و alert زیرساختی مستقل

## کنترل روز انتشار

1. متغیرهای `.env.example` را در secret manager میزبان تنظیم کنید؛ فایل `.env` را commit نکنید.
2. در staging با تنظیمات production، `./deploy.sh` را اجرا کنید.
3. `python manage.py check --deploy --settings=arvion.settings.production` را با environment واقعی اجرا کنید.
4. health، ثبت‌نام، تأیید ایمیل، بازیابی رمز و فرم عمومی/CRM را بررسی کنید.
5. یک واریز کم‌مبلغ، ثبت شماره پیگیری، رد/تأیید ادمین و فعال‌شدن دسترسی را تست کنید.
6. یک آزمون کامل، انقضا، نتیجه، ایمیل، receipt و گواهی عمومی را تست کنید.
7. دسترسی هر پنج نقش کارکنان را با حساب غیر-superuser بررسی کنید.
8. VoiceOver، صفحه‌کلید، zoom 200٪ و موبایل واقعی را روی URL staging بررسی کنید.
9. backup و rollback نسخه قبل را آماده نگه دارید.
10. پس از تأیید staging، ترافیک production را فعال و شاخص‌های خطا را زیر نظر بگیرید.

## موانع آگاهانه انتشار تجاری

- درگاه آنلاین هنوز انتخاب/متصل نشده است؛ production روش `card_transfer` را می‌پذیرد و gateway آزمایشی را رد می‌کند.
- سرویس‌های میزبانی، SMTP، S3 و پایش بیرون از مخزن‌اند و credential آن‌ها در اختیار این محیط نیست.
- هر دو بانک از نظر ساختاری و توضیح editorial گیت سخت‌گیرانه را با صفر هشدار پاس می‌کنند؛ اعتبار سطح‌بندی و شاخص‌های روان‌سنجی همچنان باید با متخصص مستقل و پایلوت آماری تأیید شود.
- پرسش‌نامه اختصاصی CRM دریافت، پالایش و در ویزارد نسخه فعلی اعمال شده است؛ بازبینی نهایی آن در جلسه discovery با نخستین مشتری واقعی انجام می‌شود.
