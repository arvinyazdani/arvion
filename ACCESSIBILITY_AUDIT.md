# ممیزی دسترس‌پذیری Rvion

استاندارد هدف: WCAG 2.1 AA — تاریخ: 2026-08-09

## جمع‌بندی

ممیزی کد قالب‌ها، CSS و تعامل منوی موبایل انجام شد. مشکلات عمده شامل کنتراست متن نارنجی روی سفید، کنتراست مرزی متن کم‌رنگ روی زمینه کاغذی، focus سراسری ناکافی، خروج focus از منوی موبایل و alt عمومی تصاویر کارت بلاگ بود. این موارد اصلاح شدند.

## اصلاحات

- رنگ مجزای `--accent-text` برای متن و لینک روشن تعریف شد؛ نارنجی برند برای سطح/پس‌زمینه و فضای تیره باقی ماند.
- رنگ muted از `#667085` به `#5f6878` تیره شد.
- focus-visible واضح با outline آبی و offset برای لینک، دکمه و کنترل فرم اضافه شد.
- منوی موبایل با Escape بسته می‌شود و Tab/Shift+Tab داخل کنترل‌های باز آن گردش می‌کند.
- مقصد skip link قابل focus شد و landmarkهای اصلی برچسب محلی دارند.
- نشانه‌های صرفاً تزئینی نوار موبایل از accessibility tree حذف شدند.
- alt تصویر بلاگ معنادار و alt تصویر fallback تزئینی خالی شد.
- برای کاربران reduced-motion حرکت hover و animation حذف شد.
- کنترل‌های لمسی روی دستگاه pointer coarse حداقل ۴۴ پیکسل ارتفاع می‌گیرند.

## بررسی کنتراست

| ترکیب | نسبت | نتیجه AA متن عادی |
|---|---:|---|
| `#5f6878` روی `#f4f1ea` | بیش از 4.5:1 | قبول |
| `#0b1220` روی `#ff6b35` | 6.6:1 | قبول |
| `#98a2b3` روی `#0b1220` | 7.27:1 | قبول |
| `#b7c0d1` روی `#0b1220` | 10.23:1 | قبول |

## محدودیت ممیزی

بررسی فعلی code-level است. پیش از انتشار عمومی، یک دور تست دستی VoiceOver روی Safari، پیمایش فقط با صفحه‌کلید، zoom 200٪ و تست دستگاه واقعی لازم است؛ نتیجه آن به رفتار مرورگر، محتوای واقعی و سرویس میزبانی وابسته است.
