# برنامه بازسازی کامل مرکز مدیریت آرویون

تاریخ: ۲۴ مرداد ۱۴۰۵

## هدف

ساخت یک وب‌اپ مدیریتی مستقل، دوزبانه، نقش‌محور و واکنش‌گرا که مدیر و همکاران برای کار روزانه به Django Admin نیاز نداشته باشند. Django Admin تا پایان مهاجرت فقط به‌عنوان مسیر اضطراری فنی باقی می‌ماند و سپس از ناوبری حذف می‌شود.

## تحلیل وضعیت موجود

- داشبورد فعلی فقط خلاصه و لینک است و بیشتر عملیات را به Django Admin می‌فرستد.
- قراردادها پوسته مستقل دارند، اما از مرکز مدیریت جدا هستند.
- درخواست همکاری، CRM، کلینیک و نیازسنجی تخصصی گردش‌کار یکپارچه ندارند.
- تأیید حساب، پرداخت کارت‌به‌کارت، تیکت، مدیریت آزمون، محتوا و گزارش‌ها هنوز به صفحات مدل‌محور Admin وابسته‌اند.
- زبان پنل مدیریت به‌صورت ساختاری از URL و ترجمه‌های یکپارچه پیروی نمی‌کند.
- نقش‌ها وجود دارند، اما منو، صفحه شروع و عملیات هر نقش به اندازه کافی متناسب نشده‌اند.
- اعلان مرورگر فعلی وابسته به بازبودن صفحه است و Push واقعی پس‌زمینه ندارد.
- داده‌ها و مدل‌های اصلی قابل استفاده‌اند؛ بازنویسی دیتابیس لازم نیست و ریسک غیرضروری ایجاد می‌کند.

## کاربران پنل و مرز دسترسی

### مدیر ارشد

دسترسی به همه عملیات، ساخت مدیر، نقش‌ها، قراردادها، پیامک، تنظیمات، گزارش‌ها و سابقه تغییرات.

### فروش و پروژه

درخواست‌های همکاری، CRM، کلینیک، نیازسنجی تخصصی، تبدیل درخواست به قرارداد و پیگیری وضعیت مشتری.

### آزمون و مالی

سفارش‌های آزمون، رسیدهای کارت‌به‌کارت، تأیید یا رد پرداخت، دسترسی آزمون، نتیجه و گواهی.

### پشتیبانی

تیکت‌ها، وضعیت سفارش و نتیجه، پاسخ و مختومه‌کردن؛ بدون دسترسی به تنظیمات مالی یا قرارداد.

### محتوا

صفحات، خدمات، پروژه‌ها و وبلاگ؛ بدون مشاهده اطلاعات مشتری، مالی یا کارکنان.

### تحلیل‌گر

بازدید، کاربران آنلاین، مسیرهای پربازدید و گزارش‌های تجمیعی؛ بدون اطلاعات محرمانه مشتری.

## معماری اطلاعات پنل جدید

1. خانه مدیریت
   - کارهای فوری، شاخص‌های امروز، اعلان‌ها، میانبرهای متناسب با نقش و فعالیت اخیر.
2. درخواست‌ها و فروش
   - درخواست همکاری، نیازسنجی CRM، نیازسنجی کلینیک، نیازسنجی تخصصی و نمای ۳۶۰ درجه مشتری.
3. قراردادها
   - ساخت از روی نیازسنجی، ویرایش اطلاعات، بندها، نسخه‌ها، لینک مشتری، بازخورد، OTP و پذیرش.
4. کاربران
   - حساب‌های مشتری، تأیید عضویت، وضعیت دسترسی و حساب کارکنان.
5. پرداخت و آزمون
   - سفارش‌ها، رسیدها، تأیید/رد، آزمون‌های فعال، نتایج، گواهی و بانک سؤال.
6. پشتیبانی
   - صندوق تیکت‌ها، اولویت، مسئول، SLA و سابقه پاسخ.
7. محتوا و سایت
   - صفحات، خدمات، پروژه‌ها، نوشته‌ها و وضعیت انتشار.
8. ارتباطات
   - ارسال پیامک تکی/گروهی، قالب‌های پیام، سابقه ارسال و اعلان‌ها.
9. گزارش‌ها
   - ترافیک، فروش، درخواست‌ها، قراردادها، پرداخت‌ها و خروجی Excel/PDF.
10. تیم و دسترسی
    - ساخت همکار، نقش‌های آماده، دسترسی‌های سفارشی محدود و Audit.
11. تنظیمات
    - اطلاعات شرکت، پرداخت، پیامک، ایمیل، اعلان Push، نگهداری داده و وضعیت سرویس‌ها.

## تجربه کاربری

- مسیر مستقل `/fa/management/` و `/en/management/` با زبان کاملاً جدا.
- Sidebar دسکتاپ با گروه‌بندی ثابت و Bottom Navigation موبایل با حداکثر پنج مقصد اصلی نقش.
- Command Palette برای جست‌وجوی مشتری، قرارداد، درخواست، کاربر و سفارش.
- صفحه‌های فهرست با جست‌وجو، فیلتر، مرتب‌سازی، pagination و عملیات گروهی.
- Drawer یا صفحه جزئیات با timeline رویداد و عملیات واضح.
- Empty state، خطا، تأیید عملیات حساس و پیام موفقیت یک‌دست.
- حذف لینک‌های مشتری از محیط مدیر؛ فقط «مشاهده سایت» در منوی حساب مدیر باقی می‌ماند.
- طراحی هماهنگ با رنگ، تایپوگرافی و مؤلفه‌های عمومی آرویون، بدون HTML تک‌خطی و CSS پراکنده.

## اعلان و کارهای فوری

- رویدادهای فوری: حساب منتظر تأیید، رسید پرداخت، بازخورد/پذیرش قرارداد، نیازسنجی جدید و تیکت بحرانی.
- Inbox داخلی با وضعیت خوانده‌شده، ارجاع‌شده و مختومه.
- Push واقعی با Service Worker و Web Push Subscription؛ بدون نیاز به بازبودن صفحه.
- یادآوری دوره‌ای فقط برای موارد حل‌نشده و با تنظیم فاصله، Quiet Hours و جلوگیری از اعلان تکراری.
- fallback پیامک یا ایمیل فقط برای دسته‌های منتخب مدیر.

## قرارداد و نیازسنجی

- تبدیل یک‌کلیکی درخواست اولیه یا تخصصی به قرارداد.
- snapshot اطلاعات منبع در قرارداد برای جلوگیری از تغییر ناخواسته بعدی.
- نمایش خلاصه پاسخ‌های نیازسنجی کنار فرم قرارداد.
- انتخاب بندها، افزودن بند، مبلغ، پرداخت، زمان‌بندی و مسئول پروژه.
- پیش‌نمایش مشتری قبل از انتشار و گردش بازخورد، اصلاح، OTP و پذیرش.

## اصول امنیتی

- کنترل دسترسی هم در view و هم query؛ پنهان‌کردن منو به‌تنهایی مجوز محسوب نمی‌شود.
- ثبت Audit برای تأیید پرداخت، تغییر نقش، انتشار قرارداد، ارسال پیامک و تغییر محتوا.
- عملیات حساس با POST، CSRF، تأیید دوباره و قفل تراکنشی.
- عدم نمایش secrets، OTP hash، اطلاعات پرداخت خام و داده‌های سایر نقش‌ها.
- تست مجوز مثبت و منفی برای هر ماژول.

## برنامه اجرایی

### مرحله ۱ — هسته و Design System

- ساخت layout مشترک دوزبانه، navigation نقش‌محور، header، sidebar، bottom nav و اجزای فرم/جدول/modal.
- تعریف URLهای زبان‌دار و redirect امن `/management/`.
- ساخت صفحه خانه جدید با command center و حذف لینک‌های مشتری.

وضعیت: اجرا شد — پوسته مشترک، مسیرهای `/fa/management/` و `/en/management/`، Sidebar، Bottom Navigation، منوی نقش‌محور، داشبورد جدید و حذف لینک مستقیم Django Admin از نمای جدید پیاده‌سازی شدند.

### مرحله ۲ — درخواست‌ها و مشتری

- فهرست و جزئیات درخواست همکاری، CRM، کلینیک و نیازسنجی تخصصی.
- تغییر وضعیت، یادداشت داخلی، خروجی متن و تبدیل به قرارداد بدون Django Admin.

وضعیت: اجرا شد — صندوق مشترک درخواست همکاری، CRM و کلینیک، جست‌وجو/فیلتر، جزئیات تماس و نیاز، دسترسی به فرم تخصصی، تغییر وضعیت و یادداشت داخلی با کنترل مجوز مستقل از Django Admin آماده است. متن‌ها و وضعیت‌های نمای انگلیسی نیز از فارسی تفکیک شدند. تست مرحله شامل ۱۷ تست خودکار موفق بود؛ اتصال مرورگر برای دور جدید تست بصری در دسترس نبود و تست بصری تولید قبلی برای فهرست درخواست‌ها بدون overflow و لینک Admin موفق ثبت شده است.

### مرحله ۳ — قرارداد کامل

- انتقال سازنده قرارداد، بندها، نسخه‌ها، لینک، بازخورد و پذیرش به پوسته جدید.
- اتصال مستقیم نیازسنجی اولیه و تخصصی به قرارداد.

وضعیت: اجرا شد — فهرست، ساخت، جزئیات، بندها و انتشار قرارداد داخل مسیر زبان‌دار مرکز مدیریت قرار گرفتند؛ پوسته قدیمی قرارداد با Design System مشترک جایگزین شد. فرم قرارداد از نیازسنجی CRM/کلینیک پر می‌شود و پاسخ‌های تکمیل‌شده نیازسنجی تخصصی CRM نیز در snapshot محدوده پروژه وارد می‌شوند. برچسب‌های فرم در نمای انگلیسی مستقل شدند و لینک عمومی مشتری و OTP بدون تغییر امنیتی باقی ماندند. تست یکپارچه قرارداد و مدیریت: ۲۶ تست موفق.

### مرحله ۴ — حساب و پرداخت

- صف تأیید حساب‌ها و رسیدهای پرداخت با عملیات تأیید/رد و ثبت Audit.
- فعال‌سازی دسترسی آزمون بدون ورود به Admin.

وضعیت: اجرا شد — صف مشترک حساب‌های منتظر تأیید و رسیدهای کارت‌به‌کارت در پنل جدید ساخته شد. تأیید حساب، تأیید/رد رسید، یادداشت بررسی، ثبت مدیر و زمان، صدور idempotent دسترسی آزمون و ارسال ایمیل اطلاع‌رسانی در همین صفحه انجام می‌شود. مدل OperationalAudit برای سابقه غیرقابل‌اختلاط عملیات حساس اضافه شد. تست یکپارچه تا این مرحله: ۲۸ تست موفق.

### مرحله ۵ — آزمون و پشتیبانی

- مدیریت کاتالوگ و نسخه آزمون، بانک سؤال، نتیجه، گواهی و صندوق تیکت.
- عملیات پرریسک انتشار سؤال و گواهی با تأیید دو مرحله‌ای داخلی.

وضعیت: بخش عملیاتی اجرا شد — مرکز مشترک آزمون و پشتیبانی، کاتالوگ و وضعیت آزمون‌ها، تلاش‌های اخیر، شمار نتایج و گواهی‌ها و صندوق تیکت نقش‌محور آماده است. وضعیت تیکت از همین پنل تغییر می‌کند و Audit دارد. سازنده کامل بانک سؤال و انتشار دومرحله‌ای به زیرمرحله تکمیلی محتوای تخصصی آزمون منتقل شد تا بدون گردش بازبینی محتوایی ناامن منتشر نشود.

### مرحله ۶ — محتوا و ارتباطات

- مدیریت خدمات، پروژه، وبلاگ و صفحات.
- مرکز پیامک، قالب‌های داخلی و سابقه ارسال.

وضعیت: اجرا شد — مرکز محتوای نقش‌محور برای مشاهده و کنترل انتشار نوشته‌ها، پروژه‌ها و خدمات و پایش صفحات ثابت ساخته شد؛ تغییر وضعیت انتشار Audit می‌شود. مرکز پیامک تکی/گروهی و سابقه ارسال نیز به پوسته دوزبانه جدید منتقل شد. نقش محتوا هیچ دسترسی به اطلاعات مشتری ندارد.

### مرحله ۷ — تیم، اعلان Push و تنظیمات

- مدیریت نقش‌ها، کارکنان، Push Subscription، Quiet Hours و سلامت اتصال‌ها.
- داشبورد گزارش و Audit قابل جست‌وجو.

وضعیت: بخش داخلی اجرا شد — ناوبری کامل نقش‌محور، تیم و دسترسی، مرکز اعلان و سابقه Audit قابل جست‌وجو همگی به پوسته جدید منتقل شدند. اعلان مرورگر با رضایت کاربر و یادآوری ساعتی هنگام فعال‌بودن وب‌اپ کار می‌کند و Service Worker کلیک اعلان را به مرکز مدیریت بازمی‌گرداند. Push واقعی پس‌زمینه برای زمان بسته‌بودن مرورگر نیازمند تولید کلید VAPID و سرویس ارسال زمان‌بندی‌شده روی سرور است و به‌عنوان تنها وابستگی بیرونی این مرحله باقی مانده است.

تکمیل Push و یادآوری: مدل اشتراک مستقل هر دستگاه، رسید مستقل هر مدیر برای هر اعلان، کلید VAPID، Push پس‌زمینه، Timer یک‌دقیقه‌ای سرور و یادآوری ساعتی پیاده‌سازی شدند. تمام اتفاق‌های تازه Push فوری دارند؛ فقط دسته پرداخت پیامک فوری می‌فرستد. بازکردن مرکز اعلان، رسیدهای قابل مشاهده را seen می‌کند و مانع یادآوری ساعتی آن‌ها می‌شود. هر اعلان جدید بعدی رسید تازه دارد و مستقل Push می‌شود. پیامک فوری و Push دارای deduplication دیتابیسی هستند و اشتراک‌های منقضی 404/410 خودکار غیرفعال می‌شوند.

انتشار Push: کامیت‌های `19e747e` و `e063c59` منتشر شدند؛ `pywebpush` روی production نصب، migration شماره ۰۰۰۵ اجرا، کلید خصوصی VAPID در `/srv/arvion/.secrets/` با دسترسی ۶۰۰ نگهداری و کلید عمومی در env ثبت شد. Timer `arvion-notifications.timer` فعال است و هر ۶۰ ثانیه اجرا می‌شود؛ اجرای واقعی پردازشگر با خروجی بدون خطا تأیید شد. شماره پیامک مدیر در env و فاصله یادآوری ۳۶۰۰ ثانیه تنظیم شد. در زمان انتشار هنوز هیچ دستگاهی اشتراک Push ثبت نکرده بود؛ مدیر باید یک‌بار از هر دستگاه دکمه «فعال‌سازی اعلان» را تأیید کند، سپس ارسال تست واقعی انجام می‌شود.

### مرحله ۸ — مهاجرت و حذف وابستگی روزمره به Admin

- تست کامل نقش‌ها، موبایل/دسکتاپ، فارسی/انگلیسی و عملیات حساس.
- انتشار تدریجی با feature flag و rollback.
- حذف لینک Django Admin از پنل و محدودکردن آن به مدیر فنی اضطراری.
- نگهداری داده و مدل‌های موجود؛ حذف فقط قالب‌ها و مسیرهای قدیمی پس از تأیید نسخه جدید.

وضعیت: اجرا شد — همه لینک‌ها و target اعلان‌های پنل جدید از Django Admin جدا شدند، قالب داشبورد قدیمی حذف شد و Django Admin فقط به‌عنوان مسیر اضطراری فنی بدون لینک در تجربه مدیر باقی ماند. کل مجموعه ۲۰۵ تست پروژه با موفقیت اجرا شد؛ `manage.py check` و بررسی migration نیز موفق بودند. انتشار شامل migration مدل Audit، collectstatic، restart و health check است. تست بصری دور نهایی پس از deploy دوباره اجرا می‌شود؛ اتصال مرورگر در نوبت قبلی موقتاً در دسترس نبود.

## گزارش انتشار بازسازی

- `b91debe`: تکمیل عملیات وضعیت و یادداشت داخلی درخواست‌ها — ۱۷ تست موفق.
- `82b0113`: انتقال قرارداد و اتصال نیازسنجی اولیه/تخصصی — ۲۶ تست موفق.
- `48b4950`: تأیید حساب و پرداخت و مدل Audit — ۲۸ تست موفق.
- `9d5be0e`: مرکز آزمون و پشتیبانی — ۲۹ تست موفق.
- `c90f323`: مرکز محتوا و ارتباطات — ۳۰ تست موفق.
- `02cbd86`: ناوبری نقش‌محور، اعلان، تیم و Audit قابل جست‌وجو.
- `83c978f`: حذف وابستگی روزمره و targetهای اعلان به Django Admin — ۲۰۵ تست کل پروژه موفق.
- انتشار production: فایل‌های tracked بدون حذف `.env`، media یا داده سرور overlay شدند؛ `collectstatic` انجام شد؛ migration `management_portal.0004_operationalaudit` به‌صورت صریح اجرا و وجود جدول با query تأیید شد؛ سرویس `arvion` فعال و `https://rvionai.com/health/` برابر ۲۰۰ است.
- تست بصری نهایی: اتصال Chrome پس از دو تلاش در دسترس نبود. تست‌های بصری قبلی پوسته مرحله ۱ و صندوق درخواست مرحله ۲ در عرض ۳۹۰ و ۱۴۴۰ بدون overflow ثبت شده‌اند؛ صفحات تازه از Design System و breakpoint مشترک همان پوسته استفاده می‌کنند، اما دور نهایی تصویری هنوز باید پس از اتصال مرورگر تکرار شود.

## معیار پذیرش نهایی

- تمام کارهای روزانه مدیر از پنل جدید قابل انجام باشد.
- هیچ عملیات اصلی به `/admin/` لینک ندهد.
- فارسی و انگلیسی مستقل و بدون متن مخلوط باشند.
- منوی هر نقش فقط ابزارهای مجاز و لازم را نمایش دهد.
- همه صفحات اصلی در عرض ۳۹۰، ۷۶۸ و ۱۴۴۰ بدون overflow و با Chrome واقعی تأیید شوند.
- اعلان Push پس‌زمینه، Audit و تست‌های permission عملیاتی باشند.
- rollback و بکاپ قبل از حذف مسیر قدیمی آماده باشد.

## تصمیم‌های فنی

- Django Templates و JavaScript سبک حفظ می‌شوند تا پیچیدگی SPA و هزینه نگهداری اضافه نشود.
- مدل‌های دامنه موجود منبع اصلی داده باقی می‌مانند؛ پنل جدید یک لایه عملیات و ارائه روی آن‌هاست.
- APIهای JSON فقط برای تعامل‌های لازم مانند جست‌وجو، اعلان، فیلتر پویا و عملیات async ساخته می‌شوند.
- Django Admin در دوره مهاجرت حذف فیزیکی نمی‌شود؛ از تجربه مدیر کنار گذاشته و بعداً محدود می‌شود.

## مرحله تکمیلی — اعلان، داشبورد و حفاظت از اسناد (۱۴۰۵/۰۵/۲۴)

- بررسی production با نشست واقعی مدیر نشان داد کنترل فعال‌سازی اعلان در نمای عملی داشبورد در دسترس نبود و تشخیص حالت `denied` نیز راه بازیابی مجوز را توضیح نمی‌داد.
- فعال‌سازی اعلان به کارت مستقل داشبورد منتقل شد؛ وضعیت‌های پشتیبانی‌نشده، مسدود، در حال اتصال، خطا و فعال به‌صورت دوزبانه نمایش داده می‌شوند. راهنمای اختصاصی آیفون/Home Screen و مرورگرهای دسکتاپ اضافه شد.
- دریافت CSRF روی صفحات صرفاً خواندنی تضمین شد تا ثبت Push Subscription پس از تأیید مجوز با خطای 403 متوقف نشود.
- خانه مدیریت با سلسله‌مراتب جدید «وضعیت فوری، اعلان، بایگانی اسناد، اولویت‌ها، صندوق کار و ابزارها» برای موبایل و دسکتاپ بازطراحی شد.
- فرم اولیه و تخصصی مشتری، خروجی آن و قرارداد، سند عملیاتی غیرقابل‌حذف تلقی می‌شوند. بازطراحی فقط لایه نمایش و دسترسی را تغییر می‌دهد و هیچ migration حذف داده یا پاک‌سازی رکورد اجرا نمی‌کند.
- نام نقش‌ها، برچسب فرم ساخت/ویرایش مدیر و فرم پیامک برای هر زبان مستقل شد و تغییر زبان، مسیر جاری و query را حفظ می‌کند.
- تست این مرحله: `manage.py check` بدون خطا و ۲۵ تست `management_portal` موفق. پس از انتشار، تست بصری آنلاین فارسی/انگلیسی در موبایل و دسکتاپ و ثبت وضعیت واقعی Notification تکرار می‌شود.
- کامیت `86c2005`: بازطراحی خانه مدیریت، کارت فعال‌سازی اعلان، بایگانی اسناد و جداسازی فرم‌های مدیریتی فارسی/انگلیسی.
- کامیت `56dfef6`: شمارنده، وضعیت ارسال و تمام متن‌های رابط مرکز پیامک در انگلیسی از متن فارسی جدا شد؛ محتوای فارسی خود مشتری ترجمه یا دستکاری نمی‌شود.
- عیب‌یابی Push آیفون: اشتراک `web.push.apple.com` فعال و ارسال سرور بدون خطا بود اما نمایش دستگاه تأیید نشد. تست محلی مستقیم از Service Worker به دکمه اعلان اضافه و آیکن اعلان به PNG سازگار با iOS تغییر کرد؛ این تست تحویل Apple را از تنظیمات نمایش خود دستگاه تفکیک می‌کند.
- علت تکمیلی: CDN آروان برای `/service-worker.js` هنوز نسخه کش‌شده `rvion-shell-v1` را تحویل می‌داد، درحالی‌که origin نسخه `v2` داشت. ثبت Worker به URL نسخه‌دار `?v=2` با `updateViaCache: none` در پوسته عمومی و مدیریتی منتقل شد تا کش CDN و HTTP مانع به‌روزرسانی آیفون نشود.
- فعال‌سازی سراسری: راهنمای Push برای مدیر در همه صفحات عمومی، حساب و مرکز مدیریت فعال شد. دستگاه iOS/Android/macOS/Windows و حالت نصب iOS تشخیص داده می‌شود؛ تا زمان وجود مجوز و Subscription معتبر، راهنمای مخصوص همان دستگاه در هر نشست نمایش داده می‌شود و پس از فعال‌سازی یک اعلان محلی تأییدی می‌فرستد.

### سیاست پشتیبان‌گیری اسناد مشتری

- PostgreSQL منبع اصلی و media ضمیمه‌ها منبع فایل هستند؛ هر دو باید در بکاپ زمان‌بندی‌شده قرار گیرند.
- بکاپ روزانه دیتابیس با نگهداری حداقل ۳۰ نسخه و بکاپ media با نسخه‌بندی پیشنهاد می‌شود؛ نسخه پشتیبان باید خارج از همان ابرک نگهداری شود.
- بازیابی آزمایشی دوره‌ای الزامی است؛ وجود فایل بکاپ به‌تنهایی معیار سلامت نیست.
- عملیات حذف سند مشتری در داشبورد عمومی مدیر ارائه نمی‌شود؛ تغییر وضعیت، بایگانی و Audit جایگزین حذف روزمره هستند.

## CRM عملیاتی واحد — مرحله بنیادین

- مدل `CustomerCase` به‌عنوان پرونده واحد روی داده‌های موجود ساخته شد؛ منبع اولیه با Generic Foreign Key حفظ می‌شود و هیچ فرم واقعی جابه‌جا یا حذف نمی‌شود.
- مدل‌های `CaseDocument`، `CaseActivity` و `CaseTask` برای اسناد نسخه‌دار، Timeline و کارهای مسئول‌دار اضافه شدند.
- migration غیرمخرب، تمام Leadها و نیازسنجی‌های CRM/کلینیک موجود را به پرونده‌های واحد متصل می‌کند و سند اولیه را نگه می‌دارد.
- فرم تخصصی CRM و قراردادهای بازخوردگرفته/تأییدشده به‌صورت خودکار به پرونده مشتری متصل می‌شوند.
- migration تکمیلی، فرم‌های تخصصی و قراردادهای موجود پیش از CRM را نیز به پرونده درست متصل می‌کند؛ قرارداد پذیرفته‌شده مرحله پرونده را به «موفق» می‌برد.
- فضای CRM شامل جست‌وجوی مشتری، مرحله، اولویت، مسئول، موعد پیگیری، وظیفه، Timeline، اسناد و خروجی متنی Audit‌شده است.
