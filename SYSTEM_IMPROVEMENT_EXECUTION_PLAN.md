# برنامه اصلاح و پایدارسازی سیستم Rvion

وضعیت: **مراحل 0 تا 5 و سیاست SLA اجرا شده‌اند؛ تصمیم‌های بیرونیِ باقی‌مانده در انتهای سند ثبت شده‌اند**
تاریخ: ۱۴۰۵/۰۵/۲۵

## هدف

تبدیل Rvion به یک سامانه‌ی یکپارچه و پایدار که مسیر مشتری از فرم نیازسنجی تا قرارداد، پرداخت، خدمت و پشتیبانی را بدون ابزارهای پراکنده مدیریت کند؛ در عین حال داده‌های واقعی مشتریان قابل بازیابی، گزارش‌پذیر و امن بمانند.

## اصول ثابت

- PostgreSQL و فایل‌های ضمیمه، منبع داده‌ی اصلی‌اند؛ هیچ migration یا پاک‌سازی داده بدون backup و امکان rollback اجرا نمی‌شود.
- `Customer` مرجع یکتای سازمان/شخص است و فرم، قرارداد، سفارش، پرداخت و تیکت تنها به آن متصل می‌شوند.
- مدیران با مرکز مدیریت Rvion کار می‌کنند، نه Django Admin؛ مجوز تغییر حساس همچنان نقش‌محور می‌ماند.
- کلیدها، رمزها، `.env` و backupها هرگز وارد Git یا پیام‌های عمومی نمی‌شوند.
- هر انتشار باید commit مشخص، تست خودکار، کنترل بصری فارسی/انگلیسی، health check و راه بازگشت داشته باشد.

## نقشه جریان عملیاتی

```text
فرم اولیه/تخصصی → مشتری و پرونده واحد → بررسی و وظیفه → قرارداد
                                             ↓
آزمون/خدمت ← تأیید پرداخت ← سفارش ← پذیرش قرارداد
                                             ↓
                                  Timeline + Inbox + اعلان مدیر
```

## مرحله 0 — ایمن‌سازی انتشار و Git

**هدف:** حذف انتشار فایل‌به‌فایل و بازگشت به deploy تکرارپذیر.

1. از درخت فعلی production و PostgreSQL archive مستقل تهیه شود.
2. تغییرات ۴۶۴‌تایی سرور دسته‌بندی شوند: تنظیمات محرمانه، داده/media، و سورس تکراری.
3. فقط با تأیید مالک، سورس `/srv/arvion` با `origin/main` همگام و فایل‌های `._*` مک پاک شوند؛ `.env.production`، `.secrets`، `media/` و `backups/` مستثنا می‌مانند.
4. یک release script واحد اجرا کند: fetch → checkout commit → migrate → collectstatic production → check → restart → health check.
5. خروجی release شامل commit، migration، زمان و مسیر backup در Audit ثبت شود.

**معیار پذیرش:** `git status` production تمیز، `HEAD == origin/main` و یک deploy آزمایشی قابل تکرار باشد.

### نتیجه اجرای مرحله 0

- با تأیید مالک، درخت production پس از archive به `origin/main` بازنشانی و AppleDoubleهای `._*` و سورس‌های تکراری پاک شدند؛ `.env.production`، `.secrets/`، `media/`، `backups/` و داده‌های PostgreSQL حفظ شدند.
- archive پیش از پاک‌سازی روی سرور با نام `arvion-working-tree-pre-git-clean-20260816-0710.tar.gz` نگه‌داری می‌شود.
- اسکریپت `ops/release.sh` اکنون قبل از release یک snapshot فشرده PostgreSQL در `backups/pre-release-*.dump` می‌سازد؛ فعلاً staging مستقل ایجاد نمی‌شود و snapshot پیش از release راه بازگشت است.

## مرحله 1 — یکپارچگی داده مشتری

1. ابزار تشخیص مشتری تکراری با اولویت موبایل نرمال‌شده، سپس ایمیل و سپس تأیید دستی نام ایجاد شود.
2. صفحه «ادغام مشتری» برای مدیر ساخته شود؛ قبل از ادغام، پیش‌نمایش همه پرونده‌ها، قراردادها، سفارش‌ها و تیکت‌ها نشان داده شود.
3. کیفیت داده با گزارش پرونده بدون مشتری، سفارش بدون مشتری، مخاطب بدون راه تماس و قرارداد بدون پرونده پایش شود.
4. Timeline دارای نوع رویداد ثابت، شناسه منبع و جلوگیری از رخداد تکراری شود.

**معیار پذیرش:** گزارش ناهماهنگی صفر یا دارای صف رسیدگی مشخص باشد؛ ادغام داده قابل Audit و برگشت‌پذیر باشد.

### نتیجه اجرای گام اول مرحله 1

- صفحه «مشتریان یکپارچه» اکنون یک ممیزی خواندنی و غیرمخرب نشان می‌دهد: پرونده بدون مشتری، سفارش بدون مشتری، مشتری بدون مخاطب، مشتری بدون شماره/ایمیل و نامزدهای تکراری با شماره یا ایمیل یکسان.
- این بخش فقط برای بررسی و اولویت‌بندی است؛ هیچ ادغام یا حذف خودکاری روی داده‌های مشتری، فرم‌های نیازسنجی یا قراردادها انجام نمی‌دهد.
- گام بعدی همین مرحله، طراحی «ادغام مشتری با پیش‌نمایش و Audit» است و فقط پس از تأیید جداگانه اجرا می‌شود.

### نتیجه اجرای گام دوم مرحله 1

- صفحه‌ی فقط‌خواندنی «بررسی تکراری‌ها» ساخته شد. مدیر گروه‌های دارای شماره یا ایمیل مشترک را انتخاب می‌کند و نام، راه تماس، تعداد مخاطب، پرونده، قرارداد و خدمت هر مشتری را کنار هم می‌بیند.
- مقایسه به هیچ عنوان ادغام، حذف یا انتقال داده انجام نمی‌دهد؛ هدف آن تصمیم آگاهانه پیش از ساخت ابزار ادغام است.

### نتیجه اجرای گام سوم مرحله 1

- ادغام کنترل‌شده در همان صفحه مقایسه اضافه شد. فقط مدیر ارشد می‌تواند آن را انجام دهد، فقط زمانی که دو مشتری شماره یا ایمیل کاملاً مشترک دارند، و فقط پس از واردکردن عبارت `MERGE`.
- پیش از تصمیم، همه داده‌های مهم هر پرونده دیده می‌شود؛ هنگام ادغام، مخاطبان، پرونده‌های کاری، قراردادها و سفارش‌ها به مشتری مقصد منتقل می‌شوند. مخاطب تکراری به‌جای تکثیر، به‌صورت امن ادغام می‌شود.
- مشتری مبدا پس از انتقال حذف می‌شود، اما رویداد Timeline برای پرونده‌های منتقل‌شده و `OperationalAudit` شامل مبدا، مقصد، مبنای تطبیق و شمار داده‌های جابه‌جاشده حفظ می‌شود.

## مرحله 2 — Inbox، کارها و SLA

1. Inbox به سه صف «فوری»، «امروز» و «اطلاع‌رسانی» تفکیک شود.
2. هر رویداد مالک، موعد و وضعیت رسیدگی داشته باشد؛ اعلان خوانده‌شده با کار انجام‌شده اشتباه گرفته نشود.
3. پرداخت در انتظار تأیید فوری باشد؛ سایر رویدادهای جدید push می‌گیرند و در صورت دیده‌نشدن فقط هر ساعت یک یادآوری می‌گیرند.
4. داشبورد نقش‌محور شاخص‌های قابل اقدام نشان دهد: تأخیر پرداخت، تیکت بدون پاسخ، قرارداد نزدیک انقضا و پیگیری سررسیدشده.

**معیار پذیرش:** هر Inbox item به یک مشتری/منبع/مسئول و اقدام بعدی قابل ردیابی باشد.

### سیاست SLA تأییدشده

- رسید کارت‌به‌کارت: حداکثر ۳۰ دقیقه برای تأیید در ساعت کاری؛ پس از عبور از مهلت، هشدار مالی فوری ایجاد می‌شود.
- تیکت باز: پاسخ اولیه حداکثر ۴ ساعت کاری؛ پس از مهلت، در Inbox پشتیبانی هشدار ایجاد می‌شود.
- فرم جدید و قرارداد ارسال‌شده: پیگیری اولیه حداکثر یک روز کاری؛ پس از مهلت، در Inbox فروش/قرارداد هشدار ایجاد می‌شود.
- هشدارهای SLA دارای `source_key` یکتا هستند؛ تکرار اجرای timer اعلان تکراری تولید نمی‌کند. Push همه رویدادها و SMS فقط پرداخت‌های فوری است.

### نتیجه اجرای گام اول مرحله 2

- صندوق عملیات به سه صف اقدام‌محور «فوری»، «امروز» و «اطلاع‌رسانی» تبدیل شد. پرداخت و هر مورد سررسیدگذشته در صف فوری، موارد دارای موعد همان روز در صف امروز و سایر رویدادها در صف اطلاع‌رسانی نمایش داده می‌شوند.
- رویدادهای تازه از این پس موعد پاسخ متناسب با SLA می‌گیرند: پرداخت ۳۰ دقیقه، پشتیبانی ۴ ساعت و فروش/قرارداد/حساب یک روز.
- هر همکار مجاز می‌تواند مسئولیت یک مورد را بپذیرد. پذیرفتن مسئولیت فقط وضعیت دیده‌شدن را به‌روزرسانی می‌کند و با «مختومه‌کردن» که پایان واقعی کار است متفاوت می‌ماند؛ این عمل در Audit ثبت می‌شود.

### نتیجه اجرای گام دوم مرحله 2

- صفحه اصلی مدیریت اکنون کارت‌های SLA نقش‌محور دارد: پرداخت خارج از مهلت، تیکت بدون پاسخ، پیگیری فروش/قرارداد سررسیدشده و وظیفه CRM عقب‌افتاده.
- هر کارت فقط برای نقش مجاز دیده می‌شود و مستقیماً به محل رسیدگی درست متصل است؛ بنابراین مدیر به‌جای شمارش خام، تأخیر قابل اقدام را می‌بیند.

## مرحله 3 — تجربه مدیریت و مشتری

1. Design system واحد برای فارسی و انگلیسی: جهت، اعداد، فونت، فاصله‌ها، رنگ وضعیت و حالت‌های خالی/خطا.
2. تست بصری اجباری برای 390، 768 و 1440 پیکسل؛ overflow افقی، CTA مبهم و ترکیب زبان مانع انتشار باشند.
3. مسیر مشتری با راهنمای مرحله‌ای برای ثبت‌نام OTP، پرداخت کارت‌به‌کارت، انتظار تأیید و ورود به آزمون بازبینی شود.
4. قرارداد و فرم تخصصی با لینک اختصاصی، پیام‌های واضح، ذخیره موقت و نمایش خطای قابل‌فهم تکمیل شوند.

**معیار پذیرش:** هر مسیر اصلی در فارسی و انگلیسی، موبایل و دسکتاپ قابل انجام و بدون نیاز به Django Admin باشد.

### نتیجه اجرای بازطراحی ناوبری مدیریت

- ناوبری مدیریت برای موبایل و دسکتاپ یک الگوی واحد گرفت: مسیرهای روزانه، کارهای نیازمند رسیدگی و تنظیمات کسب‌وکار گروه‌بندی شدند.
- عبارت‌های کوتاه و مبهم به آیکن‌های قابل تشخیص تبدیل شدند، Badge اعلان خوانده‌نشده مستقل و خوانا است و صفحهٔ فعلی با رنگ و حالت فعال مشخص می‌شود.
- در موبایل، نوار پایین فقط میانبرهای روزانه را نگه می‌دارد؛ هنگام باز بودن منو پنهان می‌شود تا با منوی کناری تداخل نداشته باشد. دکمه همبرگری نیز به دکمه بستن تبدیل و یک دکمه بستن مستقل در خود منو اضافه شد.
- تست خودکار ماژول مدیریت با ۳۵ تست موفق اجرا شد. کنترل بصری انتشار روی عرض‌های ۳۹۰ و ۱۴۴۰ پیکسل انجام می‌شود.

### نتیجه اجرای کنترل بصری مرحله 3

- صفحهٔ اصلی production در عرض‌های ۳۹۰، ۷۶۸ و ۱۴۴۰ پیکسل برای مسیرهای استاندارد `/fa/` و `/en/` بررسی شد. جهت صفحه، عنوان، متن، ناوبری و آمار هر زبان درست بود و هیچ overflow افقی مشاهده نشد.
- انتخاب زبان در Rvion با prefix آدرس انجام می‌شود، نه `?lang=en`؛ بنابراین لینک‌های اشتراکی باید به شکل `/fa/...` یا `/en/...` ساخته شوند. این الگو canonical و SEO صفحه را نیز ثابت نگه می‌دارد.

### به‌روزرسانی فرم تخصصی نوربینان

- بخش کامل «هلو و اتصال‌ها» از فرم نیازسنجی تخصصی CRM حذف شد. بنابراین فرم نوربینان هیچ سؤال یا تعهدی دربارهٔ اتصال به هلو یا نرم‌افزار حسابداری ندارد؛ سایر بخش‌های عملیاتی CRM بدون تغییر باقی می‌مانند.

### اتاق امن قرارداد نوربینان — گام آزمایشی

- قبل از نمایش هر قرارداد عمومی، ورود با شمارهٔ ثبت‌شده و کد یک‌بارمصرف الزامی شد. کد ورود و کد تأیید نهایی قرارداد هدف جداگانه دارند، یک‌بارمصرف‌اند و هر دو به نسخهٔ مشخص قرارداد متصل می‌مانند.
- نسخهٔ آزمایشی فقط برای شمارهٔ مالک ساخته می‌شود و فاقد اعتبار حقوقی است؛ فعال‌سازی شمارهٔ مشتری و متن نهایی پس از تأیید مسیر آزمایشی انجام می‌شود.
- ورود قرارداد اکنون پس از هر ارسال کد، دکمهٔ ارسال دوباره را دقیقاً برای دو دقیقه غیرفعال می‌کند و سرور نیز همین فاصله را enforce می‌کند؛ بنابراین با refresh یا ارسال مستقیم درخواست، محدودیت دور زده نمی‌شود.
- بنا بر تصمیم مالک برای مسیر آزمایشی، ورود اتاق قرارداد از OTP به «شمارهٔ مجاز + رمز محرمانهٔ محیط production» تغییر یافت. این تغییر فقط ورود به قرارداد را پوشش می‌دهد؛ OTP تأیید نهایی قرارداد و پیامک‌های دیگر سیستم حذف نشده‌اند.

## مرحله 4 — پایداری، امنیت و بازیابی

1. backup روزانه PostgreSQL و media به Object Storage آروان با retention حداقل ۳۰ روز تنظیم شود.
2. checksum، رمزنگاری مقصد و restore drill ماهانه ثبت شوند.
3. خطاهای برنامه، صف اعلان، SMS، OTP، سلامت سرویس و فضای دیسک مانیتور شوند؛ فقط رخدادهای فوری SMS مدیر بگیرند.
4. گردش کلید SMS و secret key، محدودیت دسترسی SSH و به‌روزرسانی وابستگی‌ها برنامه‌ریزی شود.

**معیار پذیرش:** بازیابی آزمایشی موفق، هشدار پیشگیرانه و عدم وجود secret در Git.

### نتیجه اجرای گام اول مرحله 4

- پایش زمان‌بندی‌شدهٔ سرور هر ۱۵ دقیقه اتصال PostgreSQL، درصد مصرف دیسک و تازگی backup را کنترل می‌کند و در صورت مشکل فقط یک اعلان قابل اقدام برای مدیر ایجاد می‌کند.
- با رفع مشکل، اعلان باز همان منبع خودکار مختومه می‌شود؛ بنابراین Inbox با هشدار تکراری پر نمی‌شود. timer اعلان و timer سلامت در release و bootstrap هر دو به‌صورت پایدار فعال می‌شوند.

### نتیجه اجرای گام دوم مرحله 4

- backup روزانهٔ محلی PostgreSQL و media ساعت ۰۲:۳۰ با تأخیر تصادفی کوتاه اجرا می‌شود و نسخه‌های روزانهٔ بیش از ۳۰ روز را فقط از مسیر مشخص backup حذف می‌کند.
- این لایه جای Object Storage نیست؛ تا زمان تعریف bucket آروان، backupها همچنان روی همان سرور قرار دارند و صرفاً بازیابی محلی را پوشش می‌دهند.

### نتیجه اجرای گام سوم مرحله 4

- restore drill ماهانه از آخرین backup PostgreSQL یک دیتابیس موقت زمان‌دار می‌سازد، `pg_restore` و یک query سلامت را اجرا می‌کند و همان دیتابیس موقت را حذف می‌کند. دیتابیس اصلی در این فرآیند تغییر نمی‌کند.

## مرحله 5 — کنترل کیفیت و انتشار پایدار

1. تست‌های واحد/یکپارچه برای فرم‌ها، قرارداد، پرداخت، OTP، تیکت، Timeline و مجوزها در CI اجرا شوند.
2. release checklist به‌صورت اسکریپت اجرایی و گزارش‌پذیر شود.
3. انتشار ابتدا در محیط آزمایشی یا snapshot سرور، سپس production با rollback commit انجام شود.
4. بعد از انتشار: health check، تست مسیرهای حساس و مرور Inbox انجام شود.

**معیار پذیرش:** هر انتشار قابل تکرار، قابل rollback و دارای گزارش تست باشد.

### نتیجه اجرای گام اول مرحله 5

- گیت CI اکنون پیش از test، همهٔ اپ‌های Django از جمله قرارداد، CRM، کلینیک، مدیریت و ترافیک را compile می‌کند؛ بنابراین خطای نحوی بخش‌های افزوده‌شده پس از نسخهٔ اولیه نیز پیش از انتشار متوقف می‌شود.

### نتیجه اجرای گام دوم مرحله 5

- اسکریپت انتشار پس از restart، مسیر سلامت را از داخل سرور با Host واقعی production بررسی می‌کند و فقط در صورت پاسخ موفق، یک رکورد بدون اطلاعات محرمانه در `backups/release-history.log` می‌نویسد. هر رکورد شامل زمان، commit منتشرشده، مسیر snapshot پیش از انتشار و نتیجهٔ health check است؛ بنابراین نسخهٔ live و نقطهٔ بازگشت آن قابل پیگیری است.
- خواندن commit در اسکریپت با `safe.directory` محدود به همان مسیر production انجام می‌شود تا اجرای release با `sudo` در سرور متوقف نشود؛ این تنظیم هیچ مسیر دیگری را trusted نمی‌کند.
- بررسی سلامت پس از restart تا ۱۵ ثانیه retry محدود دارد تا زمان واقعی بالا آمدن Gunicorn به‌عنوان خطای انتشار ثبت نشود؛ بدون پاسخ موفق، release همچنان ناموفق می‌ماند و گزارش success نمی‌سازد.
- خطای اتصالِ موقتیِ تلاش‌های retry در خروجی عادی پنهان است و فقط پس از پایان ۱۵ تلاش، خطای روشن و عملیاتی ثبت می‌شود؛ بنابراین لاگ release فقط failure واقعی را برجسته می‌کند.

### نتیجه اجرای گام سوم مرحله 5

- مجموعهٔ کامل ۲۲۵ تست Django با اجرای موازی، بدون خطا اجرا شد و بررسی سخت‌گیرانهٔ بانک سؤال نیز گذشت. بنابراین مسیرهای فرم، قرارداد، پرداخت، OTP، تیکت، Timeline و سطح دسترسی‌ها پیش از پایان این مرحله یک‌بار دیگر در محیط محلی تأیید شده‌اند.

### نتیجه اجرای گام چهارم مرحله 5

- راهنمای عملیاتی فارسی برای اجرای تست پیش از انتشار، انتشار استاندارد، کنترل پس از انتشار، معیار rollback و حدود امن restore در `docs/OPERATIONS_RUNBOOK_FA.md` اضافه شد. این راهنما مسیرهای واقعی production، snapshot خودکار و ریسک backup خارج از سرور را شفاف می‌کند.

## اولویت اجرا

### اتاق اختصاصی قرارداد نوربینان — بازطراحی مسیر مشتری

- اتاق قرارداد از سایت اصلی جدا شد: برند، خروج امن و تمام مسیرهای بازگشت درون همان پرونده باقی می‌مانند.
- نیازسنجی تخصصی CRM با session امن قرارداد باز می‌شود، در هر مرحله پاسخ را ذخیره می‌کند و پس از تکمیل به اتاق قرارداد برمی‌گردد.
- شرایط عمومی و خصوصی به‌صورت جداگانه خوانده و تأیید می‌شوند. هر تأیید به نسخهٔ غیرقابل‌تغییر قرارداد، زمان، اثر انگشت IP و مرورگر متصل است؛ بنابراین قبل از تأیید نهایی، مسیر بررسی قابل ممیزی خواهد بود.
- مرحلهٔ نهایی فقط پس از بررسی دو سند حقوقی قابل دسترس است. خروج از اتاق فقط session همین پرونده را حذف می‌کند و به سایر ورودهای کاربر آسیب نمی‌زند.
- کنترل کد و تست‌های قرارداد و CRM پس از این بازطراحی بدون خطا انجام شد. انتشار production و ثبت متن‌های نهایی، پس از کنترل نهایی متن عمومی و خصوصی نوربینان انجام می‌شود.
- برای جلوگیری از عبور ناخواسته از ترتیب حقوقی، ثبت نظر دربارهٔ بندهای نهایی تا زمان ثبت بررسی هر دو سند عمومی و خصوصی در سطح سرور هم قفل است؛ صرفاً پنهان‌کردن دکمه در رابط کاربری نیست.
- در تست live، مسیر فرم تخصصی یک خطای session داشت: به‌جای شناسهٔ دیتابیس نسخه از شمارهٔ نسخه استفاده می‌کرد و مشتری را به اتاق قرارداد بازمی‌گرداند. این خطا اصلاح شد و مسیر فرم تخصصی هم به‌صورت صریح فارسی تولید می‌شود تا انتخاب زبان قبلی مرورگر، رابط قرارداد فارسی را مخلوط نکند.
- تست واقعی ثبت بررسی سند، خطای CSRF در HTTPS را آشکار کرد: تنظیم `no-referrer` روی صفحهٔ دارای فرم باعث حذف Referer مورد نیاز Django می‌شد. سیاست Referrer به `same-origin` تغییر کرد؛ هیچ Referrer به مقصد خارجی ارسال نمی‌شود و ارسال فرم داخلی معتبر می‌ماند.
- همان کنترل CSRF برای فرم درخواست/ثبت OTP تأیید نهایی نیز اعمال شد. هدر آن از سایت اصلی جدا شد و متن انگلیسیِ مرحلهٔ تأیید نهایی به فارسی تبدیل شد.
- بنا بر تصمیم مالک، OTP و ارسال SMS از جریان قرارداد به‌طور کامل حذف شد. ورود شماره و رمز همچنان کنترل دسترسی است؛ تأیید نهایی با checkbox صریح، شمارهٔ ورود، زمان و اثر انگشت نسخه ثبت می‌شود.
- صفحهٔ ورود پرونده قرارداد بازطراحی شد: فیلد شماره و رمز اکنون ارتفاع و حالت focus یکسان دارند، رمز قابل نمایش/پنهان‌کردن است، نام پرونده و عنوان پروژه دیده می‌شود و متن/راهنمای فارسی و مناسب موبایل جایگزین عبارت‌های پراکندهٔ قبلی شد.
- کنترل نهایی live نشان داد فرم «خروج امن» نیز به‌علت `no-referrer` در HTTPS با CSRF متوقف می‌شود. این صفحه هم به `same-origin` تغییر کرد و تست CSRF خروج امن افزوده شد.
- پس از ثبت تأیید نهایی، دکمهٔ صریح «بازگشت به پرونده قرارداد» نمایش داده می‌شود تا مشتری در جریان جداگانهٔ پرونده باقی بماند.
- قرارداد مشتری اکنون سه مرحلهٔ مستقل دارد: شرایط عمومی، شرایط خصوصی و بررسی نهایی. ورود به شرایط خصوصی تا ثبت بررسی عمومی قفل است و ورود به بررسی نهایی تا ثبت هر دو سند قفل می‌ماند. متن‌های حقوقی با تیتر ماده، شمارهٔ بند و فاصله‌گذاری خوانا نمایش داده می‌شوند؛ بعد از هر ثبت، مشتری به منوی پرونده برمی‌گردد.
- مرحلهٔ قدیمی «بررسی تک‌تک بندهای پیشنهادی» حذف شد. تأیید نهایی اکنون مستقیم در منوی پرونده است و فقط با تکمیل فرم تخصصی، شرایط عمومی و شرایط خصوصی فعال می‌شود. پس از تیک موافقت و ثبت، session قرارداد پاک می‌شود، پیام پایان نمایش داده می‌شود و مشتری به صفحه ورود برمی‌گردد.

| اولویت | مرحله | دلیل |
|---|---|---|
| P0 | مرحله 0 و 4 | جلوگیری از انتشار ناامن و از دست‌رفتن داده |
| P1 | مرحله 1 و 2 | یکپارچگی واقعی کار مدیر و مشتری |
| P2 | مرحله 3 | بهبود محسوس تجربه و نرخ تکمیل فرایندها |
| P3 | مرحله 5 | بلوغ کامل چرخه انتشار |

## تصمیم‌های باقی‌مانده از مالک

1. نام bucket و دسترسی محدود Object Storage آروان برای backup خارج از سرور؛ این مورد فعلاً با تصمیم مالک به تعویق افتاده است.
2. زمان ساعت کاری رسمی تیم، در صورت نیاز به محاسبه دقیق SLA بر مبنای روز و ساعت کاری به‌جای زمان تقویمی.
