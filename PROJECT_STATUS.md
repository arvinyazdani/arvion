# سند زنده پروژه Rvion

آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۱۸ (2026-08-09)

## هدف و درک پروژه

Rvion یک پلتفرم دوزبانه فارسی/انگلیسی مبتنی بر Django است که سه مسیر اصلی دارد:

1. معرفی شرکت، خدمات، پروژه‌ها و محتوای تخصصی برای جذب مشتری؛
2. دریافت و مدیریت درخواست‌های همکاری و سرنخ‌های فروش؛
3. فروش و اجرای آزمون‌های آنلاین، گزارش نتیجه، گواهی قابل‌اعتبارسنجی و پشتیبانی پس از آزمون.

کاربر می‌تواند حساب تأییدشده بسازد، آزمون بخرد یا در محیط محلی رایگان فعال کند، پاسخ‌ها را با ذخیره خودکار ثبت کند، نتیجه و برنامه یادگیری بگیرد و سوابق خصوصی خود را ببیند. کارکنان با نقش‌های حداقلی فروش، محتوا، آزمون و پشتیبانی از Django Admin استفاده می‌کنند.

## معماری فعلی

- `accounts`: ثبت‌نام، تأیید ایمیل، ورود، بازیابی رمز، پروفایل و داشبورد خصوصی
- `assessments`: کاتالوگ، سفارش، پرداخت، اجرای آزمون، نمره، سلامت آزمون، نتیجه، گواهی و تیکت
- `leads`: فرم عمومی درخواست همکاری و قیف اولیه فروش
- `services`: خدمات قابل انتشار
- `projects`: نمونه‌کارها
- `blog`: محتوای دوزبانه
- `core`: صفحات عمومی، اطلاعات شرکت، SEO، پوسته سایت و داشبورد عملیاتی
- تنظیمات مجزای local و production، PostgreSQL در production، رسانه S3-compatible و static با WhiteNoise

## کارهای انجام‌شده پیش از این برنامه

- تجربه دوزبانه، صفحات شرکت و حقوقی، SEO و sitemap
- حساب تأییدشده و بازیابی امن رمز
- موتور آزمون نسخه‌دار با بانک‌های سؤال انگلیسی و Python/Django
- انتخاب متوازن سؤال، snapshot، ذخیره پاسخ، محدودیت زمانی و رخدادهای سلامت
- گزارش، برنامه یادگیری، گواهی و اعتبارسنجی عمومی گواهی
- سفارش، رسید، ایمیل‌های تراکنشی، پشتیبانی و قرارداد تأیید پرداخت امن
- فرم هدایت‌شده خدمات و نقش‌های عملیاتی ادمین
- پوسته mobile-first و گیت محلی انتشار

## برنامه اجرایی جاری

- [x] مرحله ۱ — سخت‌سازی امنیت و تست مرزهای دسترسی
- [x] مرحله ۲ — اعتبارسنجی محتوایی و ساختاری بانک سؤال‌ها
- [x] مرحله ۳ — تست مسیرهای کامل کاربر و CI
- [x] مرحله ۴ — ممیزی و اصلاح WCAG 2.1 AA
- [x] مرحله ۵ — ویزارد مستقل سفارش CRM سازمانی
- [x] مرحله ۶ — گیت نهایی آمادگی انتشار و مستندات تحویل
- [x] مرحله ۷ — تکمیل کیفیت editorial بانک‌های سؤال
- [x] مرحله ۸ — پرداخت کارت‌به‌کارت با تأیید ادمین
- [x] مرحله ۹ — پایش بازدید و کاربران آنلاین
- [x] مرحله ۱۰ — بسته استقرار و راه‌اندازی سرور

سه وابستگی بیرونی که پیاده‌سازی‌شان نیازمند اطلاعات/حساب سرویس‌دهنده است: درگاه پرداخت واقعی، زیرساخت میزبانی production و سرویس پایش/تحلیل. کد باید برای اتصال آن‌ها آماده باشد، اما اسرار یا سرویس ساختگی جایگزین آن‌ها نمی‌شود.

## معیار پذیرش

- هر مرحله یک commit مستقل و قابل بازگشت دارد.
- همین فایل در هر commit با تغییرات، تست‌ها و محدودیت‌های آن مرحله به‌روز می‌شود.
- `release-check.sh` در انتها بدون تغییر مخرب داده اجرا می‌شود.
- هیچ ادعای «اعتبار علمی» برای آزمون‌ها بدون بازبینی متخصص انسانی درج نمی‌شود.
- سؤال‌های اختصاصی CRM کاربر پس از دریافت، از نظر ضرورت، ابهام، تکرار و قابلیت تبدیل به نیازمندی طراحی بازبینی می‌شوند.

## گزارش اجرا

### آغاز برنامه

- دامنه کار و معیار پذیرش ثبت شد.
- وضعیت اولیه مخزن تمیز بود و شاخه `main` نسبت به `origin/main` دارای commitهای محلی است.
- سؤال‌های اختصاصی CRM هنوز دریافت نشده‌اند؛ پرسش‌نامه پایه ساخته می‌شود و پس از دریافت سؤال‌ها دور دوم بازبینی انجام خواهد شد.

### مرحله ۱ — سخت‌سازی امنیت

- ورود با کلید ترکیبی IP و ایمیل محدود شد؛ شمارنده پس از ورود موفق پاک می‌شود.
- درخواست‌های بازیابی رمز و ارسال دوباره تأیید ایمیل بدون افشای وجود حساب محدود شدند.
- هدرهای production برای referrer، جداسازی پنجره، SameSite و HttpOnly صریح شدند.
- تصویر نمونه‌کار مانند تصویر بلاگ به پسوندهای تصویری امن و سقف یک مگابایت محدود شد.
- متغیرهای قابل تنظیم محدودسازی در `.env.example` ثبت شدند.
- تست‌های امنیتی ورود ناموفق، قفل موقت و پاک‌شدن شمارنده اضافه شدند.
- تصمیم آگاهانه: `X-Forwarded-For` برای rate limit پذیرفته نمی‌شود، چون بدون فهرست proxyهای مورد اعتماد قابل جعل است.
- محدودیت باقی‌مانده: MFA ادمین به انتخاب ارائه‌دهنده هویت/OTP و فرایند بازیابی سازمانی نیاز دارد و نباید با یک پیاده‌سازی صوری جایگزین شود.

### مرحله ۲ — ممیزی بانک سؤال

- هر ۴۰۰ سؤال منبع از نظر ساختار، کلید، گزینه‌ها، بخش، دشواری، پوشش مهارت و توضیحات بررسی شدند.
- کنترل جدید برخورد گزینه‌ها پس از یکسان‌سازی حروف/فاصله، توضیح‌های ناقص، prompt نزدیک به تکراری و پوشش محدود دشواری اضافه شد.
- فرمان `audit_question_banks` و حالت سخت‌گیرانه `--strict-editorial` ساخته و به گیت انتشار افزوده شد.
- نتایج و مرز اعتبار این بررسی در `QUESTION_BANK_AUDIT.md` ثبت شد.
- تناقض قطعی در کلید پاسخ نمونه‌های بازبینی‌شده پیدا نشد، اما توضیح‌های قالبی به‌درستی هشدار می‌گیرند؛ انتشار تجاری با ادعای سطح معتبر همچنان منوط به بازبینی متخصص مستقل و پایلوت آماری است.

### مرحله ۳ — مسیر کاربر و CI

- workflow کنترل کیفیت GitHub برای Python 3.11 و 3.12 اضافه شد.
- CI با حداقل دسترسی، timeout و لغو اجرای قدیمی، compile، check جنگو، drift مهاجرت، همه تست‌ها و ممیزی بانک سؤال را اجرا می‌کند.
- دو تست journey سطح HTTP اضافه شد: ثبت‌نام تا تأیید ایمیل و ورود به داشبورد؛ ثبت درخواست عمومی تا صفحه کد پیگیری.
- مسیرهای حساس آزمون، پرداخت، مالکیت نتیجه و receipt از قبل تست integration دارند و در همان suite اجرا می‌شوند.
- محدودیت: تست مرورگر واقعی برای رفتار JavaScript نیازمند runner دارای Playwright و browser binary است؛ CI فعلی بدون افزودن وابستگی سنگین، مسیرهای سروری را end-to-end کنترل می‌کند.

### مرحله ۴ — دسترس‌پذیری

- ممیزی WCAG 2.1 AA در `ACCESSIBILITY_AUDIT.md` ثبت شد.
- کنتراست متن، focus-visible، هدف لمسی، reduced motion، alt تصاویر و landmarkهای محلی اصلاح شدند.
- focus در منوی موبایل باز محصور و بستن با Escape حفظ شد.
- تست پوسته برای skip link، مقصد focus و ویژگی‌های ARIA اضافه شد.
- تست دستی نهایی با VoiceOver و دستگاه واقعی پس از بالا آمدن URL عمومی در چک‌لیست باقی می‌ماند.

### مرحله ۵ — سفارش CRM سازمانی

- اپ مستقل `crm_orders` با مدل، migration، فرم، view، URL، template، JavaScript، admin و تست ساخته شد؛ داده‌ها با Lead عمومی مخلوط نمی‌شوند.
- ویزارد پنج‌مرحله‌ای فارسی برای سازمان، مسئله، دامنه، داده/فناوری و تصمیم پروژه ساخته شد.
- پاسخ‌های چندگزینه‌ای به‌شکل ساختاریافته و توضیح فرآیندها به‌شکل متن قابل تحلیل ذخیره می‌شوند.
- اعتبارسنجی سمت سرور، honeypot، rate limit پنج‌دقیقه‌ای، رضایت حریم خصوصی، کد پیگیری تصادفی و ایمیل داخلی اضافه شد.
- نقش فروش فقط مجوز مشاهده/ویرایش سفارش CRM می‌گیرد و حذف یا ساخت مستقیم ندارد؛ کارت سفارش‌های جدید نیز به داشبورد عملیاتی افزوده شد.
- منطق انتخاب و حذف پرسش‌ها و روش بازبینی سؤال‌های ارسالی کاربر در `CRM_DISCOVERY_GUIDE.md` ثبت شد.
- تست‌ها ثبت کامل، تبدیل رقم فارسی، داده ساختاریافته، پاسخ کوتاه، ربات و دسترسی نقش فروش را پوشش می‌دهند.
- QA مرورگر روی دسکتاپ و viewport موبایل ۳۹۰×۸۴۴ انجام شد؛ overflow افقی وجود نداشت و مشکل scroll/focus ناخواسته بارگذاری اولیه پیدا و اصلاح شد.

### مرحله ۶ — آماده‌سازی نهایی انتشار

- گیت محلی با بررسی dependency، migration drift، همه تست‌ها، ممیزی سؤال، collectstatic و benchmark تکمیل شد.
- timeout ارسال ایمیل production صریح شد تا worker در خرابی SMTP بی‌نهایت منتظر نماند.
- چک‌لیست staging، روز انتشار، rollback و وابستگی‌های بیرونی در `FINAL_RELEASE_CHECKLIST.md` ثبت شد.
- اجرای نهایی `release-check.sh` موفق بود: ۱۳۵ تست در ۳۸.۹ ثانیه، بدون migration drift یا dependency ناسازگار، collectstatic موفق و benchmark صد آزمون با throughput حدود ۳۵.۸ workflow در ثانیه و p95 حدود ۴۲.۱ میلی‌ثانیه.
- ممیزی ۴۰۰ سؤال از نظر ساختاری پاس شد و ۲۵۰ هشدار editorial باقی ماند (۵۰ انگلیسی و ۲۰۰ Python/Django). این هشدارها مانع فنی اجرای سامانه نیستند، اما مانع ادعای اعتبار محتوایی نهایی‌اند و در چک‌لیست انتشار تجاری حفظ شده‌اند.
- درخواست fallback مرورگر برای `/favicon.ico` که در QA محلی 404 می‌شد به favicon SVG موجود هدایت شد.

### بازبینی پرسش‌نامه CRM ارسالی — 2026-08-09

- هر ۳۴ سؤال ارسالی با معیار اثر بر دامنه، معماری، مهاجرت، امنیت، زمان و هزینه بازبینی شد؛ ماتریس تصمیم در `CRM_DISCOVERY_GUIDE.md` ثبت شد.
- ویزارد با نوع مشتری، منبع جذب، داده پرونده، یادآوری، اعلان، مکاتبات شرطی، AI اختیاری، گزارش‌ها، نقش‌ها، دستگاه، اپ موبایل، integration، مهاجرت، audit، راهبرد اجرا و خدمات مجری تکمیل شد.
- بازه‌های اندازه سازمان، کاربران، زمان و بودجه با متن ارسالی هماهنگ شدند.
- سؤال‌های فهرستیِ جهت‌دهنده درباره مشکل فعلی و مراحل فروش به پاسخ تشریحی فرآیندمحور تبدیل شدند.
- migration افزایشی نوشته شد و سفارش‌های قبلی حذف یا بازنویسی نمی‌شوند.
- کنترل تناقض گزینه‌های «هیچ‌کدام» و تست مسیرهای جدید اضافه شد.
- QA مرورگر نسخه نخستِ ادغام‌شده ۱۳۸ checkbox را نشان داد؛ برای جلوگیری از خستگی فرم، جزئیات کم‌اثر به جلسه منتقل و گزینه‌های مشابه گروه‌بندی شدند.
- نسخه پالایش‌شده ۸۱ checkbox در کل پنج مرحله دارد (گزینه‌های دبیرخانه نیز شرطی‌اند)؛ QA موبایل ۳۹۰ پیکسل بدون overflow افقی موفق بود.
- گیت انتشار پس از این بازبینی با ۱۳۷ تست موفق، بدون migration drift و benchmark صد workflow با throughput حدود ۳۷.۱ در ثانیه و p95 حدود ۳۴.۶ میلی‌ثانیه دوباره اجرا شد.

### مرحله ۷ — کیفیت editorial بانک سؤال

- ۵۰ سؤال پایه انگلیسی از حالت توضیح قالبی خارج و برای هرکدام rationale آموزشی متناسب با همان سؤال نوشته شد.
- `BANK_VERSION` انگلیسی از ۳ به ۴ افزایش یافت تا نسخه منتشرشده قبلی دست‌کاری نشود.
- تست seed به نسخه منبع متصل شد و دیگر شماره نسخه را hard-code نمی‌کند.
- ممیزی ۲۰۰ سؤال انگلیسی اکنون صفر هشدار editorial دارد؛ ۲۰۰ هشدار Python/Django مرحله بعدی این برنامه است.
- ۲۰۰ سؤال Python/Django نیز rationale دوزبانه وابسته به بخش، زیرمهارت، صورت سؤال و پاسخ کلیدی گرفتند.
- `BANK_VERSION` فنی از ۲ به ۳ افزایش یافت و نسخه ۲ تاریخی دست‌نخورده می‌ماند.
- ممیزی سؤال فنی اکنون حداقل طول آموزشی و ذکر صریح پاسخ کلیدی را کنترل می‌کند.
- گیت `audit_question_banks --strict-editorial` برای هر دو بانک با مجموع ۴۰۰ سؤال و صفر هشدار موفق است.
- CI و `release-check.sh` از حالت گزارش هشدار به حالت سخت‌گیرانه تغییر کردند؛ بازگشت explanation قالبی از این پس انتشار را متوقف می‌کند.
- گیت کامل نهایی موفق بود: ۱۳۸ تست در ۴۵ ثانیه، ۴۰۰ سؤال با صفر هشدار editorial، بدون migration drift یا dependency ناسازگار، collectstatic موفق و benchmark صد workflow با throughput حدود ۳۵.۴ در ثانیه و p95 حدود ۴۲.۶ میلی‌ثانیه.

## محدودیت‌ها و تصمیم‌های باز

### مرحله ۸ — پرداخت کارت‌به‌کارت با تأیید ادمین

- شماره کارت و نام دارنده از متغیرهای محیطی خوانده می‌شود و مقدار عملیاتی فعلی برای کارت `6219861821208849` به نام آروین یزدانی ثبت شده است.
- مشتری نام واریزکننده، شماره پیگیری، تاریخ و ساعت پرداخت را ثبت می‌کند؛ ارقام فارسی یکسان‌سازی و زمان آینده یا قدیمی‌تر از ۳۰ روز رد می‌شود.
- ثبت اطلاعات پرداخت هیچ دسترسی‌ای ایجاد نمی‌کند. رکورد در صف بررسی ادمین می‌ماند و ایمیل اطلاع‌رسانی داخلی ارسال می‌شود.
- اقدام ادمین برای تأیید، در تراکنش دیتابیس و از مسیر idempotent موجود اجرا می‌شود؛ سپس دقیقاً یک مجوز آزمون صادر و نتیجه برای مشتری ایمیل می‌شود. اقدام رد نیز بدون صدور دسترسی ثبت می‌شود.
- درگاه آنلاین در صفحه پرداخت با وضعیت «در دست اقدام · موقتاً در حال تعمیر» نمایش داده می‌شود.
- migration شماره ۰۰۱۵، دسترسی نقش عملیات آزمون و تست‌های عدم صدور دسترسی پیش از تأیید، تأیید ادمین و زمان نامعتبر اضافه شدند.

### مرحله ۹ — مانیتورینگ داخلی و حریم خصوصی

- اپ مستقل `traffic` نمایش صفحات عمومی، بازدیدکننده یکتای روزانه و بازدیدکنندگان فعال پنج دقیقه اخیر را ثبت می‌کند.
- شناسه بازدیدکننده از کلید نشست و secret سامانه hash می‌شود؛ IP و query string ذخیره نمی‌شوند و مسیرهای admin، health، static و media از آمار حذف‌اند.
- داشبورد عملیاتی سه شاخص امروز و جدول روند ۱۴ روز را فقط به نقش مجاز analytics نشان می‌دهد.
- فرمان `cleanup_traffic_data` شناسه روزانه قدیمی‌تر از ۳۰ روز و نشست فعال قدیمی‌تر از ۲۴ ساعت را حذف می‌کند و آمار تجمیعی را نگه می‌دارد.
- متن حریم خصوصی با دامنه داده، دوره نگهداری و ماهیت first-party این پایش هماهنگ شد.
- تست‌ها شمارش page view، یکتایی نشست، حذف مسیرهای سیستمی و پاک‌سازی بدون حذف aggregate را پوشش می‌دهند.

### مرحله ۱۰ — بسته استقرار آروان‌کلاد

- پیشنهاد ظرفیت برای حالت دیتابیس جدا و حالت all-in-one در `DEPLOYMENT_ARVAN.md` ثبت شد.
- اسکریپت Ubuntu وابستگی‌ها، virtualenv، migration، نقش‌ها، بانک سؤال، static، systemd، Nginx و health check را در یک اجرا آماده می‌کند.
- سرویس Gunicorn با restart خودکار و hardening پایه و timer روزانه پاک‌سازی داده‌های موقت مانیتورینگ اضافه شد.
- چک‌لیست انتشار با جریان واقعی کارت‌به‌کارت، نقش پنجم analytics و مرز مانیتورینگ داخلی/زیرساختی به‌روز شد.
- تست قدیمی که تعداد نقش‌ها را چهار مورد hard-code کرده بود با نقش analytics هماهنگ شد.
- گیت کامل انتشار پس از این سه قابلیت موفق بود: ۱۴۳ تست، بدون migration drift یا dependency ناسازگار، ممیزی سخت‌گیرانه ۴۰۰ سؤال، collectstatic و benchmark صد workflow.
- حالت bootstrap اولیه بدون credential بیرونی اضافه شد: PostgreSQL و media روی همان ابرک اجرا می‌شوند و ایمیل موقتاً در journal سرور ثبت می‌شود. فعال‌سازی SMTP و Object Storage واقعی همچنان به کلید صادرشده از ارائه‌دهنده نیاز دارد.
- فایل User Data دارای رمزها و یادداشت credential عمداً در `.gitignore` قرار گرفتند و نباید به مخزن عمومی push شوند.
- نخستین استقرار واقعی روی ابرک Ubuntu آروان انجام شد: PostgreSQL 16، migrationها، پنج نقش کارکنان، ۴۰۰ سؤال، static، Gunicorn، Nginx، timer پاک‌سازی و superuser عملیاتی ساخته شدند.
- race راه‌اندازی اولیه Gunicorn با `--retry-connrefused` و نیاز به reload شدن Nginx پس از جایگزینی config در bootstrap اصلاح شد.
- overrideهای صریح SSL/cookie/HSTS به production افزوده شدند تا IP فقط در دوره کوتاه پیش از اتصال DNS قابل استفاده باشد؛ مقادیر پیش‌فرض همچنان HTTPS اجباری و امن باقی می‌مانند.
- دسترسی static در استقرار واقعی اصلاح شد: Nginx عضو گروه محدود پروژه می‌شود، پوشه ریشه پروژه `750` و خروجی collectstatic با directory `755` و file `644` ارائه می‌شود؛ CSS، فونت و JavaScript روی IP عمومی همگی پاسخ ۲۰۰ دارند.

### اصلاح تجربه فرم CRM و ثبت‌نام بدون SMTP — ۱۹ مرداد ۱۴۰۵

- علت برگشت فرم CRM به مرحله ۲ مشخص شد: چهار پاسخ در سرور حداقل ۲۰ کاراکتر می‌خواستند، اما این قاعده در اعتبارسنجی مرورگر تعریف نشده بود. محدودیت مرورگر و سرور همسان شد، راهنمای کوتاه کنار فیلدها اضافه شد و ارسال نامعتبر اکنون با خلاصه روشن خطا، تعداد موارد و شماره مرحله نمایش داده می‌شود؛ پاسخ‌های واردشده حفظ می‌شوند.
- حالت موقت `MANUAL_ACCOUNT_APPROVAL` اضافه شد. در این حالت ثبت‌نام حساب غیرفعال می‌سازد، هیچ ایمیل یا توکن تأییدی تولید نمی‌شود و صفحه نتیجه صریحاً روند تأیید مدیر را توضیح می‌دهد. ادمین از داشبورد حساب‌های منتظر را می‌بیند و با اقدام گروهی «تأیید و فعال‌سازی» دسترسی ورود می‌دهد.
- متن اصلی صفحه خانه از معرفی فنی کلی به بیان مسئله و نتیجه مشتری تغییر کرد و مسیرهای «مشاوره اولیه رایگان» و «نیازسنجی CRM سازمانی» مستقیماً در بخش اول قرار گرفتند. هیچ نمونه‌کار یا ادعای تأییدنشده‌ای به سایت اضافه نشد.

### بازطراحی پنل مدیریت فارسی — ۱۹ مرداد ۱۴۰۵

- عنوان‌ها، ناوبری و بخش‌های اصلی ادمین فارسی و بر اساس کار واقعی گروه‌بندی شدند: کاربران و دسترسی‌ها؛ پرداخت، آزمون و پشتیبانی؛ فروش، سفارش و CRM؛ محتوا؛ و آمار.
- صفحه کاربران از مجوزهای ریز و پیچیده پاک‌سازی شد. ادمین می‌تواند حساب مشتری را فعال کند یا نقش‌های آماده فروش و CRM، آزمون و پرداخت، و پشتیبانی را با عملیات گروهی بدهد؛ نقش آماده، ورود کارکنان به پنل را نیز فعال می‌کند.
- صف واریز کارت‌به‌کارت و صف کاربران جدید راهنمای برجسته دارند. تأیید پرداخت همان مسیر تراکنشی قبلی را اجرا می‌کند و هم‌زمان سفارش را پرداخت‌شده و دسترسی آزمون را فعال می‌کند؛ رد پرداخت همچنان دسترسی ایجاد نمی‌کند.
- داشبورد عملیات اکنون روی «کارهای نیازمند رسیدگی» تمرکز دارد و لینک مستقیم صف حساب جدید، پرداخت معلق، سفارش CRM، درخواست مشتری و پشتیبانی را بر اساس مجوز هر کارمند نشان می‌دهد.

### سیستم طراحی و نمودارهای پنل مدیریت — ۱۹ مرداد ۱۴۰۵

- فونت محلی Vazirmatn برای کل ادمین فعال شد و سیستم بصری مشترک شامل رنگ، فاصله، گوشه‌ها، سایه، حالت focus و واکنش‌گرایی موبایل در فایل مستقل `admin.css` پیاده شد.
- صفحه فهرست، فرم ویرایش، فیلترها، جدول‌ها، دکمه‌ها، پیام‌ها و نوار کناری برای خوانایی فارسی و انجام سریع عملیات بازطراحی شدند؛ حرکت‌ها نیز تنظیم reduced-motion سیستم را رعایت می‌کنند.
- نام مدل‌ها و فیلدهای عملیاتی بدون تغییر ساختار دیتابیس به فارسی نمایش داده می‌شوند و دسته‌بندی کاری قبلی حفظ شده است.
- داشبورد عملیات با کارت‌های فشرده صف کار و نمودار دوخطی ۱۴ روزه برای «نمایش صفحه» و «بازدیدکننده یکتا» تکمیل شد. نمودار از Canvas داخلی و بدون کتابخانه یا ارسال داده به سرویس ثالث ساخته می‌شود و جدول متنی قابل‌دسترسی به‌عنوان جایگزین دارد.

### خانه موبایل، اعتمادساز و PWA — ۱۹ مرداد ۱۴۰۵

- هیرو و بخش «چه کاری انجام می‌دهیم» برای نمایش موبایل بازطراحی شدند: متن کوتاه‌تر، CTAهای واضح، کارت‌های چهارمرحله‌ای قابل ورق‌زدن و بدون اسکرول افقی ناخواسته.
- نوار اعتماد فقط ادعاهای قابل اثبات را نمایش می‌دهد: تعداد واقعی پروژه‌های منتشرشده، هویت حقوقی قابل استعلام، سه ماه پشتیبانی کد و پرداخت مرحله‌ای ۵۰/۵۰. عدد ساختگی پروژه یا رضایت مشتری وارد نشد.
- PWA با service worker ریشه، کش محدود صفحات عمومی، manifest کامل، دکمه نصب و آیکن‌های ۱۹۲ و ۵۱۲ پیکسل با نشان R و زمینه نارنجی آماده شد. مسیرهای ادمین، حساب و پرداخت عمداً cache نمی‌شوند.
- تصویر اختصاصی ۱۲۰۰×۶۳۰ برای Open Graph تولید و متادیتای تصویر، ابعاد، alt و Twitter Card به همه صفحات عمومی اضافه شد تا واتساپ و پیام‌رسان‌های سازگار پیش‌نمایش تصویری نشان دهند.

### یکسان‌سازی برند و خروجی متنی CRM — ۱۹ مرداد ۱۴۰۵

- نام نمایشی فارسی برند در صفحات، ایمیل‌ها، متادیتا، PWA و ادمین از «رویون» به «آرویون» اصلاح شد؛ نام انگلیسی `Rvion` و نام حقوقی ثبت‌شده «آروین توسعه تجارت هوشمند» بدون تغییر باقی ماندند. نام‌های فنی زیرساخت برای جلوگیری از اختلال سرور تغییر نکردند.
- گزارش متنی UTF-8 کامل برای سفارش CRM اضافه شد. مقادیر ذخیره‌شده فنی مانند `sales`، `pipeline` و آرایه‌های JSON در خروجی به عنوان‌های فارسی قابل‌خواندن تبدیل می‌شوند و پاسخ‌ها در شش بخش منظم قرار می‌گیرند.
- ادمین می‌تواند داخل هر سفارش با دکمه «دریافت گزارش متنی کامل» فایل همان مشتری را بگیرد یا در فهرست سفارش‌ها چند مورد را انتخاب و با عملیات گروهی یک فایل تجمیعی دریافت کند.

### انتخاب تاریخ شمسی پرداخت کارت‌به‌کارت — ۱۹ مرداد ۱۴۰۵

- ورودی تاریخ میلادی مرورگر در فرم فارسی پرداخت حذف و با فهرست تاریخ شمسی جایگزین شد. گزینه‌ها از «امروز» تا ۳۰ روز گذشته را با عدد و نام ماه فارسی نمایش می‌دهند.
- تاریخ انتخاب‌شده در سرور به میلادی استاندارد تبدیل و سپس همراه ساعت تهران در دیتابیس ذخیره می‌شود؛ محدودیت زمان آینده و پرداخت قدیمی همچنان برقرار است.
- تبدیل دوطرفه جلالی/میلادی داخل پروژه و بدون وابستگی یا سرویس خارجی پیاده شد و تاریخ مرجع ۱۹ مرداد ۱۴۰۵ در تست پوشش داده شد.

### بروزرسانی خودکار تأیید پرداخت — ۱۹ مرداد ۱۴۰۵

- صفحه انتظار پرداخت کارت‌به‌کارت هر ۵ ثانیه وضعیت سفارش همان کاربر را به‌صورت امن بررسی می‌کند.
- پس از تأیید ادمین، پیام موفقیت نمایش داده می‌شود و کاربر خودکار به داشبورد آماده‌سازی آزمون منتقل می‌شود.
- در صورت رد پرداخت، پیام روشن برای تماس با پشتیبانی نمایش داده می‌شود و انتقال انجام نمی‌شود.
- پاسخ وضعیت قابل کش شدن نیست و کاربر به وضعیت سفارش اشخاص دیگر دسترسی ندارد.

### تجربه نصب PWA و خوش‌آمد برند — ۱۹ مرداد ۱۴۰۵

- دکمه ساده نصب با راهنمای واکنش‌گرا و دستگاه‌محور جایگزین شد؛ محتوای iPhone/iPad، Android و دسکتاپ به‌صورت جداگانه و متناسب با مرورگر نمایش داده می‌شود.
- در مرورگرهای پشتیبان، نصب مستقیم PWA از همان راهنما انجام می‌شود و در iOS/Safari مراحل دستی Share، Add to Home Screen و Add توضیح داده شده است.
- راهنمای نصب در حالت اجرای مستقل اپ نمایش داده نمی‌شود و پس از رویداد نصب نیز در مرورگر پنهان می‌ماند.
- صفحه خوش‌آمد تمام‌صفحه با لوگوی Rvion، حلقه‌های چرخان و نوار بارگذاری سبک اضافه شد؛ فقط یک بار در هر نشست نمایش داده می‌شود و برای کاهش حرکت سیستم حالت جایگزین دارد.

### اصلاح حساب مشتری در موبایل و زبان — ۱۹ مرداد ۱۴۰۵

- زبان داشبورد اکنون از پیشوند معتبر URL گرفته می‌شود؛ در نتیجه `/en/account/dashboard/` حتی با ترجیح ذخیره‌شده فارسی، محتوای انگلیسی صحیح نشان می‌دهد.
- کلید تغییر زبان در ابتدای داشبورد حساب، مستقل از منوی موبایل و با حفظ همان مسیر صفحه اضافه شد.
- نام و ایمیل طولانی، کارت‌های آزمون، عملیات حساب، تاریخچه خرید و تاریخچه نتایج برای جلوگیری از بیرون‌زدگی و اسکرول افقی محدود و بازچینش شدند.
- در عرض موبایل، دکمه‌های آزمون و لینک‌های حساب تمام‌عرض، کارت‌ها فشرده‌تر و تیترها خواناتر می‌شوند؛ جهت ایمیل نیز مستقل از زبان صفحه چپ‌به‌راست باقی می‌ماند.

### صفحه فقط‌خواندنی معرفی اپ CRM — ۱۹ مرداد ۱۴۰۵

- محتوای فایل معرفی CRM به یک صفحه مستقل، دوزبانه و هماهنگ با پوسته آرویون در مسیر `/fa/crm/` و `/en/crm/` تبدیل شد؛ فایل خام یا CSS آن مستقیماً اجرا نشده است.
- صفحه شامل ارزش محصول، ۹ قابلیت فعال، مخاطبان مناسب، مدل استقرار مستقل، معماری فنی و مسیرهای توسعه سفارشی است.
- صفحه فرم یا عملیات تغییردهنده داده ندارد و لینک‌های محتوایی آن فقط به بخش امکانات همان صفحه و فرم نیازسنجی داخلی آرویون متصل‌اند.
- دسترسی «معرفی اپ CRM» کنار دکمه نیازسنجی صفحه اصلی و کنار سفارش CRM در منوی سایت اضافه و صفحه در نقشه سایت ثبت شد.

### انتقال دامنه اصلی به rvionai.com — ۱۹ مرداد ۱۴۰۵

- دامنه‌های `rvionai.com` و `www.rvionai.com` به فهرست میزبان‌های مجاز Django، مبادی مورد اعتماد CSRF و پیکربندی Nginx اضافه شدند؛ IP سرور برای دوره گذار حفظ شد.
- آدرس مرجع سایت، canonical، hreflang، sitemap و دامنه نمایشی پروفایل شرکت به `https://rvionai.com` منتقل شدند.
- مهاجرت داده برای بروزرسانی دامنه پروفایل شرکت و امکان بازگشت کنترل‌شده اضافه شد؛ اسکریپت ساخت سرور جدید نیز از دامنه تازه استفاده می‌کند.
- پاسخ عمومی HTTP دامنه و health check از شبکه آروان با کد ۲۰۰ تأیید شد. اجبار HTTPS تا فعال‌شدن گواهی لبه آروان خاموش می‌ماند تا سایت قطع نشود.

### HTTPS سرتاسری دامنه — ۱۹ مرداد ۱۴۰۵

- Nginx برای TLS روی پورت ۴۴۳، پروتکل‌های TLS 1.2/1.3 و مسیر استاندارد ACME آماده شد تا ارتباط آروان تا origin نیز رمزنگاری شود.
- اسکریپت deploy hook برای اتصال خودکار گواهی تمدیدشده Let’s Encrypt و reload امن Nginx اضافه شد.
- گواهی معتبر Let’s Encrypt برای `rvionai.com` و `www.rvionai.com` روی origin صادر و به Nginx متصل شد؛ تمدید آزمایشی و timer خودکار موفق‌اند.
- ریدایرکت دائمی HTTP به HTTPS، Session/CSRF Cookie امن و HSTS یک‌ساله برای دامنه اصلی و زیردامنه‌ها فعال و پاسخ عمومی HTTPS با کد ۲۰۰ تأیید شد.

### پایداری ورود و خطای CSRF — ۱۹ مرداد ۱۴۰۵

- خطای گزارش‌شده با حساب آزمایشی بررسی شد؛ حساب با دامنه تایپی `gmail.con` ثبت شده بود و ورود تازه همان حساب روی HTTPS با موفقیت به داشبورد رسید. خطای قبلی با فرم یا کوکی قدیمی زمان گذار HTTPS سازگار بود.
- پاسخ‌های ورود و ثبت‌نام `no-cache` شدند تا مرورگر یا لایه واسط فرم دارای CSRF قدیمی را نگه ندارد. حفاظت CSRF فعال و بدون کاهش سطح امنیت باقی ماند.
- صفحه خام ۴۰۳ با پیام دوزبانه و امن جایگزین شد که منقضی‌شدن نشست را توضیح می‌دهد و کاربر را به فرم ورود تازه هدایت می‌کند؛ این پاسخ نیز قابل کش نیست.
- ثبت‌نام با اشتباه رایج `gmail.con` رد می‌شود و فرم پیشنهاد روشن `gmail.com` می‌دهد تا حساب با ایمیل غیرقابل‌استفاده ساخته نشود.

- نام و مستندات API درگاه پرداخت باید پس از انتخاب سرویس‌دهنده مشخص شود.
- دامنه، ارائه‌دهنده PostgreSQL/S3/SMTP و مقصد استقرار هنوز در مخزن تعیین نشده‌اند.
- اعتبار محتوایی نهایی آزمون زبان نیازمند متخصص آموزش زبان و پایلوت آماری است؛ بررسی این برنامه یک ممیزی فنی/زبانی دقیق است، نه صدور اعتبارنامه روان‌سنجی.
