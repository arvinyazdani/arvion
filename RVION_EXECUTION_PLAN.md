# دفتر اجرای نهایی آرویون

این فایل مرجع پشتیبان برنامه اجرایی آرویون است. پس از پایان هر مرحله، نتیجه، آزمون‌ها و شناسه کامیت در همین فایل ثبت می‌شود. اطلاعات محرمانه، رمزها و کلیدهای API هرگز در این سند نوشته نمی‌شوند.

## وضعیت کلی

| مرحله | موضوع | وضعیت |
|---|---|---|
| ۱ | بازطراحی صفحه اصلی و منو | انجام شد |
| ۲ | داشبورد مدیریتی مستقل | انجام شد |
| ۳ | مدیران، مسئولان و دسترسی‌ها | انجام شد |
| ۴ | پیشنهاد و نسخه‌بندی قرارداد | انجام شد |
| ۵ | پذیرش قرارداد با OTP ملی‌پیامک | پیاده‌سازی شد؛ فعال‌سازی واقعی منتظر BodyId |
| ۶ | اعلان‌ها و گردش کار مدیریت | انجام شد |
| ۷ | کنترل کیفیت، امنیت و محتوا | انجام شد |
| ۸ | انتشار نهایی روی سرور | انجام شد |

## مرحله ۱ — بازطراحی صفحه اصلی و منو

هدف: صفحه اول باید در چند ثانیه حوزه کار آرویون را توضیح دهد و کاربر را بدون روبه‌روشدن با تعداد زیادی دکمه به مسیر درست ببرد. منوی موبایل به گروه‌های «راهکارها»، «آشنایی» و «حساب و ارتباط» تقسیم می‌شود؛ دو اقدام اصلی هیرو، شروع پروژه و مشاهده راهکارها هستند.

معیار پایان: نبود اسکرول افقی در عرض ۳۹۰ پیکسل، دکمه‌های لمسی حداقل ۴۴ پیکسل، منوی قابل استفاده با صفحه‌کلید، متن دوزبانه و عبور تست‌های صفحات عمومی.

نتیجه اجرا:

- هیرو از پنج اقدام هم‌زمان به دو اقدام «شروع پروژه» و «مشاهده راهکارها» کاهش یافت.
- سه مسیر CRM سازمانی، پلتفرم کلینیک و سامانه اختصاصی در کارت‌های مستقل با توضیح و اقدام روشن قرار گرفتند.
- راهنمای نصب اپ از هیرو خارج و به بخش کم‌اولویت‌تر زیر راهکارها منتقل شد.
- منوی موبایل به سه گروه راهکارها، آشنایی با آرویون و حساب و ارتباط تقسیم شد؛ لینک‌های تکراری دسکتاپ حذف شدند.
- اجزای جدید از توکن‌های موجود رنگ، فاصله، شعاع و دکمه استفاده می‌کنند و در breakpointهای ۸۲۰ و ۵۲۰ پیکسل تک‌ستونه می‌شوند.
- ۲۳ تست صفحات عمومی و سفر کاربر موفق بودند و `django check` و `git diff --check` خطایی نداشتند.
- تست تصویری خودکار به دلیل نبود مرورگر متصل در محیط اجرا نشد و برای مرحله ۷ باز نگه داشته شد؛ بنابراین نتیجه بصری نهایی هنوز نیازمند مشاهده واقعی است.

## مرحله ۲ — داشبورد مدیریتی مستقل

هدف: ساخت محیط فارسی و واکنش‌گرا خارج از Django Admin برای مشاهده صف کارها، کاربران، سفارش‌ها، پرداخت‌ها، قراردادها و آمار. عملیات پرتکرار باید بدون ورود به مدل‌های خام انجام شوند.

معیار پایان: فقط کارکنان مجاز وارد شوند؛ اعداد از دیتابیس واقعی بیایند؛ صف‌های نیازمند اقدام لینک مستقیم و وضعیت روشن داشته باشند.

نتیجه اجرا:

- مرکز مدیریت مستقل در مسیر `/management/` با پوسته فارسی، واکنش‌گرا و `noindex` ساخته شد.
- کارت‌های تأیید حساب، درخواست همکاری، نیازسنجی CRM و کلینیک، پرداخت، تیکت، آزمون فعال و ترافیک فقط مطابق مجوز همان کارمند نمایش داده می‌شوند.
- صندوق کار موارد تازه را از چند بخش یکپارچه و بر اساس زمان مرتب می‌کند؛ اطلاعات بخشی که کارمند مجوزش را ندارد حتی در شمارنده‌ها نمایش داده نمی‌شود.
- آمار بازدید امروز، کاربران آنلاین پنج دقیقه اخیر و نمودار هفت‌روزه برای نقش تحلیل در نظر گرفته شد.
- مسیر مدیریت پیشرفته به مرکز جدید متصل شد.
- چهار تست ورود، منع کاربر عادی، جداسازی داده بر اساس مجوز و نمایش مدیر ارشد موفق بودند؛ `django check` و بررسی diff نیز موفق بود.

## مرحله ۳ — مدیران، مسئولان و دسترسی‌ها

هدف: مدیر ارشد بتواند برای فروش، پشتیبانی، محتوا، مالی و تحلیل مسئول بسازد یا نقش او را تغییر دهد. دسترسی‌ها بر اساس اصل کمترین مجوز و نقش‌های موجود پروژه اعمال می‌شوند.

معیار پایان: ساخت، غیرفعال‌سازی و تغییر نقش با ثبت عامل و زمان؛ جلوگیری از ارتقای غیرمجاز؛ پوشش تست مجوزها.

نتیجه اجرا:

- فهرست مدیران و مسئولان، فرم ساخت همکار و فرم تغییر مسئولیت داخل مرکز مدیریت اضافه شد.
- فقط superuser می‌تواند این صفحات را باز کند؛ کارکنان عادی پاسخ ۴۰۳ می‌گیرند و هیچ‌کس از این مسیر به superuser ارتقا داده نمی‌شود.
- نقش‌های ثابت فروش، آزمون و پرداخت، پشتیبانی، محتوا و تحلیل از یک منبع واحد همگام می‌شوند و مجوز دلخواه از فرم قابل انتخاب نیست.
- حساب تازه با ایمیل تأییدشده، وضعیت فعال و رمز عبور مطابق اعتبارسنجی Django ساخته می‌شود. قطع دسترسی مدیریتی حساب را حذف نمی‌کند.
- تغییر نقش حساب‌های superuser از این گردش ممنوع است تا قفل‌شدن یا تصاحب مدیر ارشد رخ ندهد.
- ۴۱ تست مرکز مدیریت و حساب‌ها، شامل منع دسترسی، ساخت نقش حداقلی و حفاظت مدیر ارشد موفق بودند.

## مرحله ۴ — پیشنهاد و نسخه‌بندی قرارداد

هدف: مدیر با واردکردن مشخصات مشتری، دامنه، مبلغ، روش پرداخت و زمان‌بندی یک پیشنهاد استاندارد بسازد. بندها به‌صورت پیش‌فرض فعال‌اند و هر تغییر، نسخه قابل حسابرسی ایجاد می‌کند. لینک مشتری تصادفی، قابل ابطال، دارای انقضا و `noindex` خواهد بود.

معیار پایان: مشتری نسخه نهایی را می‌بیند؛ بند ردشده و دلیل آن ثبت می‌شود؛ بند پیشنهادی مشتری تا تأیید مدیر قابل امضا نیست؛ محتوای پذیرفته‌شده بعداً تغییر نمی‌کند.

نتیجه اجرا:

- ساخت پیشنهاد از مرکز مدیریت با مشخصات مشتری، شماره همراه، شرح پروژه، مبلغ ریالی، روش پرداخت و زمان تحویل اضافه شد.
- دوازده بند استاندارد نرم‌افزاری به‌صورت پیش‌فرض فعال‌اند؛ مدیر می‌تواند هر بند را خاموش یا بند اختصاصی اضافه کند.
- انتشار، snapshot مستقل با شماره نسخه و SHA-256 می‌سازد. ویرایش پیش‌نویس یا انتخاب بندها نسخه قبلی را تغییر نمی‌دهد.
- لینک مشتری با توکن تصادفی ۳۲‌بایتی، انقضای پیش‌فرض ۱۴ روز، `noindex/noarchive` و جلوگیری از cache ساخته می‌شود؛ پیش‌نویس و لینک منقضی یا باطل‌شده ۴۰۴ می‌دهند.
- مشتری هر بند را جداگانه بررسی می‌کند. رد بند بدون دلیل پذیرفته نمی‌شود و بند پیشنهادی مشتری صرفاً بازخورد است، نه بخشی از نسخه قابل پذیرش.
- IP به‌صورت هش، user-agent، زمان بازخورد و شناسه بندهای پذیرفته/ردشده برای حسابرسی ثبت می‌شوند.
- متن استاندارد به‌عنوان پیش‌نویس عملیاتی علامت‌گذاری شده و پیش از استفاده نهایی به بازبینی مشاور حقوقی ایران نیاز دارد.

## مرحله ۵ — پذیرش قرارداد با OTP ملی‌پیامک

هدف: تأیید شماره همراه و پذیرش دقیق یک نسخه قرارداد با کد شش‌رقمی. کد فقط به‌صورت هش ذخیره می‌شود، پنج دقیقه اعتبار دارد، سه درخواست در ده دقیقه و پنج تلاش ورود دارد.

پیش‌نیاز بیرونی: شناسه قالب تأییدشده آرویون (`MELIPAYAMAK_BODY_ID`). API Key فقط در محیط سرور نگهداری می‌شود.

نتیجه اجرا:

- پذیرش نهایی فقط برای نسخه جاری، پس از تأیید همه بندها و نبود بند پیشنهادی حل‌نشده باز می‌شود.
- مشتری پیش از درخواست کد باید موافقت صریح با همان نسخه را علامت بزند؛ کد فقط به شماره ذخیره‌شده قرارداد ارسال می‌شود.
- کد شش‌رقمی با `secrets` تولید و فقط به‌صورت hash مقاوم Django ذخیره می‌شود؛ متن کد در دیتابیس، log یا پنل ادمین وجود ندارد.
- اعتبار پنج دقیقه، سه درخواست در ده دقیقه، پنج تلاش ناموفق، مصرف یک‌باره و جلوگیری از تأیید دوباره اعمال شد.
- پذیرش به snapshot و hash دقیق نسخه، شماره تأییدشده، شناسه ملی‌پیامک، زمان، IP هش‌شده و user-agent متصل است.
- تست با backend کنسولی و کد کنترل‌شده انجام شد و هیچ پیامک واقعی نفرستاد. ارسال واقعی تا تنظیم `MELIPAYAMAK_BODY_ID` عمداً fail-safe باقی می‌ماند.

## مرحله ۶ — اعلان‌ها و گردش کار مدیریت

هدف: ثبت‌نام جدید، تأیید حساب، رسید پرداخت، سفارش CRM/کلینیک و رویداد قرارداد در صندوق اعلان دسته‌بندی شوند. اعلان وضعیت خوانده‌نشده، در حال رسیدگی و مختومه دارد و به مسئول مرتبط نمایش داده می‌شود.

نتیجه اجرا:

- عضویت کاربر، درخواست همکاری، سفارش CRM، سفارش کلینیک، رسید پرداخت، تیکت، بازخورد قرارداد و پذیرش قرارداد به‌صورت خودکار اعلان می‌سازند.
- `source_key` یکتا از اعلان تکراری برای یک رویداد جلوگیری می‌کند.
- اعلان‌ها در دسته‌های حساب، فروش، پرداخت، پشتیبانی و قرارداد و وضعیت‌های خوانده‌نشده، خوانده‌شده و مختومه قابل فیلترند.
- کارکنان فقط اعلان نقش خود را می‌بینند و مدیر ارشد همه اعلان‌ها را می‌بیند؛ تغییر وضعیت اعلان خارج از نقش با ۴۰۴ محدود می‌شود.
- مختومه‌کردن، مدیر مسئول و زمان رسیدگی را ثبت می‌کند. شمارنده خوانده‌نشده نیز به داشبورد اصلی اضافه شد.
- تست تولید اعلان حساب، تفکیک فروش/پرداخت، مختومه‌کردن و دسترسی مدیر ارشد موفق بود.

## مرحله ۷ — کنترل کیفیت، امنیت و محتوا

هدف: آزمون گردش‌های اصلی، مجوزها، OTP، CSRF، نشست، rate limit، موبایل و محتوای مشتری‌پسند. ادعاهای اعتمادساز فقط در صورت قابل اثبات بودن نمایش داده می‌شوند.

تست بصری در این مرحله اجباری است و باید حداقل در عرض‌های ۳۹۰، ۷۶۸ و ۱۴۴۰ پیکسل برای صفحه اصلی، منوی باز، مرکز مدیریت، فرم مدیر، قرارداد مشتری و ورود OTP انجام شود. تا زمانی که مرورگر واقعی متصل نباشد، این مرحله کامل اعلام نمی‌شود.

نتیجه اجرا:

- مجموعه کامل ۱۸۷ تست موفق شد؛ migration drift وجود ندارد، وابستگی شکسته پیدا نشد و `django check` بدون خطا بود.
- ممیزی سخت‌گیرانه بانک سؤال برای ۲۰۰ سؤال انگلیسی و ۲۰۰ سؤال Python/Django با صفر هشدار تحریری موفق شد.
- benchmark صد تلاش آزمون با rollback، میانگین حدود ۳۰ میلی‌ثانیه و throughput بالاتر از ۳۳ تلاش در ثانیه را ثبت کرد.
- تست بصری واقعی در Chrome برای عرض‌های ۳۹۰، ۷۶۸ و ۱۴۴۰ انجام شد: صفحه اصلی، منوی باز موبایل، داشبورد مدیر، سند قرارداد ۱۲ بندی و صفحه OTP بررسی شدند.
- همه صفحات بررسی‌شده `scrollWidth` برابر عرض viewport داشتند و اسکرول افقی مشاهده نشد. منوی موبایل قابل اسکرول و همه بندهای قرارداد پیش‌فرض روشن بودند.
- تست بصری دو ایراد یافت و اصلاح شد: مسیر فونت Vazirmatn در صفحات مدیریت/قرارداد ۴۰۴ می‌داد؛ و `min()` در flex-basis کارت پروژه موبایل اعمال نمی‌شد و کارت را حدود ۱۱۰ پیکسل می‌کرد. فونت اکنون بدون console error بارگیری و کارت موبایل حدود ۳۳۵ پیکسل است.
- تیتر ایمیل بلند مدیر برای wrap امن و اندازه واکنش‌گرا اصلاح شد و لینک‌های مستقیم قراردادها و مدیران به داشبورد اضافه شدند.
- تصاویر مرجع در پوشه `visual-tests/` ذخیره شدند؛ داده و حساب استفاده‌شده فقط در دیتابیس محلی تست قرار دارد.

## مرحله ۸ — انتشار نهایی روی سرور

هدف: پشتیبان‌گیری، migration، جمع‌آوری static، restart کنترل‌شده، health check و آزمون سناریوهای واقعی. کلیدها در Git قرار نمی‌گیرند و برای هر انتشار مسیر بازگشت تعریف می‌شود.

نتیجه اجرا — ۲۰ مرداد ۱۴۰۵:

- پیش از انتشار، کد و PostgreSQL production در `/srv/backups/arvion-20260811-110823/` پشتیبان‌گیری شدند؛ `source.tar.gz` و `database.dump` با دسترسی محدود موجودند.
- به دلیل working tree قدیمی و dirty سرور، انتشار با بسته مستقل و بدون `.git`، `.env`، media، دیتابیس محلی و secretها انجام شد؛ از merge یا reset خطرناک استفاده نشد.
- API Key ملی‌پیامک مستقیماً از فایل محلی به `.env.production` منتقل شد، مقدار آن در خروجی یا Git نمایش داده نشد و فایل موقت سرور حذف شد.
- backend ملی‌پیامک، نام کاربری، شماره فرستنده و محدودیت‌های OTP در production فعال‌اند. `MELIPAYAMAK_BODY_ID` خالی است و تا دریافت شناسه قالب، OTP واقعی عمداً ارسال نمی‌شود.
- `check --deploy` موفق بود؛ migrationهای `contracts` و `management_portal` اعمال، پنج نقش کارکنان همگام و staticها جمع‌آوری شدند.
- سرویس‌های `arvion` و `nginx` پس از restart فعال‌اند. خانه و health عمومی HTTP 200، داشبورد HTTP 302 به ورود امن و CSS مدیریت HTTP 200 دادند.
- origin خانه حدود ۰٫۰۷ ثانیه و health حدود ۰٫۰۱ ثانیه پاسخ داد؛ درخواست گرم عمومی حدود ۲٫۲ ثانیه بود و journal سرویس warning جدیدی نداشت.
- trigger بازگشت: شکست health، خطای 5xx پایدار، شکست ورود/ثبت سفارش/پرداخت یا migration. rollback کد از `source.tar.gz` و rollback داده فقط با restore کنترل‌شده `database.dump` انجام می‌شود.

## سوابق کامیت

- `0cf6540` — زیرساخت امن ملی‌پیامک، backend تستی و تنظیمات OTP اضافه شد؛ ۵ تست موفق.
- کامیت مرحله ۱ — بازطراحی معماری اطلاعات صفحه اصلی و منوی واکنش‌گرا، همراه با بروزرسانی تست‌ها و این دفتر اجرا.
- کامیت مرحله ۲ — مرکز مدیریت مستقل، صف عملیاتی permission-aware، آمار و دسترسی سریع کارکنان.
- کامیت مرحله ۳ — ساخت و ویرایش همکار با نقش‌های حداقلی ثابت و محافظت از مدیر ارشد.
- کامیت مرحله ۴ — سازنده قرارداد، بندهای انتخابی، snapshot نسخه‌دار، لینک اختصاصی و بازخورد مشتری.
- کامیت مرحله ۵ — پذیرش نسخه قرارداد با OTP هش‌شده، محدودیت درخواست/تلاش و سابقه قابل حسابرسی.
- کامیت مرحله ۶ — اعلان‌های رویدادمحور، تفکیک بر اساس نقش، فیلتر و گردش خواندن/مختومه‌کردن.
- کامیت مرحله ۷ — آزمون کامل و بصری سه breakpoint، اصلاح فونت، کارت پروژه موبایل و دسترسی‌های مستقیم داشبورد.
- کامیت مرحله ۸ — ثبت انتشار production، بکاپ، migration، تنظیم ملی‌پیامک و smoke test عمومی.

## تکمیل پس از برنامه اصلی — ۲۰ مرداد ۱۴۰۵

- اصلاح هم‌پوشانی افکت نارنجی هوم و بازطراحی فشرده فوتر موبایل/دسکتاپ.
- متادیتا و تصویر اشتراک اختصاصی برای نیازسنجی CRM، نیازسنجی کلینیک، قرارداد خصوصی و آزمون‌ها.
- استفاده مجدد از لودر برند برای تمام ناوبری‌های داخلی و submitهای معتبر.
- مرکز ارسال پیامک تکی/گروهی مدیر ارشد با اعتبارسنجی شماره، ثبت نتیجه هر گیرنده و محدودیت ۲۰ مقصد در هر درخواست.
- وابستگی‌های بیرونی باقی‌مانده: BodyId ملی‌پیامک، SMTP، Object Storage، مانیتورینگ بیرونی، انتخاب درگاه، بازبینی حقوقی قرارداد و پایلوت تخصصی آزمون.

نتیجه انتشار ۲۳ مرداد ۱۴۰۵:

- نسخه `2806c60` پس از بکاپ سورس و PostgreSQL و تأیید checksum روی production منتشر شد.
- migration تاریخچه پیامک و staticهای چهار تصویر اشتراک اعمال شدند؛ health و سرویس‌ها سالم ماندند.
- QA واقعی Chrome برای هوم موبایل/دسکتاپ، فوتر، ناوبری دارای لودر، فرم CRM و مرکز پیامک مدیر موفق و بدون خطای console بود.

## اصلاح تکمیلی زبان صفحه خانه — ۲۳ مرداد ۱۴۰۵

- کارت Delivery به پیام نتیجه‌محور و کاملاً دوزبانه تبدیل شد و اعداد فارسی ناخواسته از نسخه انگلیسی حذف شدند.
- شاخص‌های ثبت شرکت، پشتیبانی و شیوه پرداخت نیز در نسخه انگلیسی به متن و عدد لاتین تبدیل شدند.
- ایمیل عمومی موقت از فوتر و JSON-LD حذف شد؛ فعال‌سازی ایمیل سامانه پس از دریافت تنظیمات SMTP مستقل انجام می‌شود.
- هر ۱۹۳ تست و `django check` موفق بودند. نسخه `6866e2f` روی production منتشر و در Chrome واقعی با عرض ۳۹۰ و ۱۴۴۰ بررسی شد؛ هر دو زبان بدون overflow و خطای console بودند و ایمیل موقت نمایش داده نشد.
- تبدیل رقم در لایه رابط برای کل صفحه زبان‌محور شد تا همه اعداد ثابت و پویا در انگلیسی لاتین و در فارسی فارسی نمایش داده شوند.
- مستندات REST فولدر نمونه بررسی شد؛ ارسال متن آزاد و OTP بدون BodyId فعال شد و فقط در صورت انتخاب صریح حالت `pattern` قالب پنل لازم خواهد بود.
- اتصال نیازسنجی‌های CRM/کلینیک به پیش‌نویس قرارداد با اعتبارسنجی و تست workflow اضافه شد.
- مرکز مدیریت برای موبایل نوار میانبر نقش‌محور و اعلان مرورگر با feed اختصاصی دریافت کرد؛ فعال‌سازی اعلان با اقدام صریح مدیر انجام می‌شود و تکرار پایش یک‌دقیقه‌ای از سمت کلاینت است.
- نیازسنجی تخصصی پس از فرم اولیه CRM طراحی و منتشر شد؛ ساختار سؤال‌ها در `crm_orders/specialist.py` قابل توسعه است و پاسخ‌های هر مشتری در `CrmSpecialistDiscovery` ذخیره می‌شوند. migration و static production با موفقیت اعمال و تست بصری موبایل انجام شد.
